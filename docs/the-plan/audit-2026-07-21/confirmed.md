## [critical/better] 10-security.md §10.3 (with 04-topology.md §4.2, 05-schemas.md §5.5 V3)
CLAIM: The v1 write-enforcement model cannot be implemented as specified on GitHub: §4.2 allows direct pushes to main for ungated changes, but CODEOWNERS and required status checks only act on pull requests, so on the permitted direct-push path nothing host-side prevents system A's repo-wide PAT from writing into system B's section, and V3's 'push blocked (branch protection)' is impossible (checks run post-push).
WHY: §10.3's '✘ {CODEOWNERS}' for cross-section writes and T-2/T-5's 'section scope limits writes' are the core security claims; a buggy or compromised agent (fine-grained PAT is contents-RW on the whole repo, §10.5) can silently rewrite another system's artifacts and events on main, forging lifecycle state — detected only after the fact by V4 flags. AC-101.2/CC-060 only test the PR path, so tests would go green while the hole ships.
REC: Pick one preventive mechanism and specify it: (a) PR-only writes with auto-merge for ungated changes, plus a required V3 status check that verifies changed paths ⊆ author's section; or (b) GitHub push rulesets with per-section file-path restriction and per-team bypass actors. Update §10.3 enforcement braces, T-2/T-5, and add a CC for the direct-push cross-section attempt.
VERIFY-NOTE: Confirmed. Chain: §3.7 gate list (G1–G5) does not include ordinary "write own section artifacts/events" → §2.2 "branch protection with required review where a gate applies" ⇒ that action gets no required review → §4.2 "Sections whose gates don't apply push directly to main; gated changes go through PR review" — so the normal write path is a direct push, not a PR. CODEOWNERS ("write other section | ✘ {CODEOWNERS}", §10.3) only activates via "Require review from Code Owners," a PR-merge-time GitHub feature; it has zero effect on a direct push. §10.5's PAT is "contents RW on the specific space repos only" — repo-scoped, not path-scoped (GitHub fine-grained PATs can't restrict by directory), so the credential legitimately used for own-section writes can write anywhere in the repo. §5.5's V3 row claims "merge/push blocked (branch protection)" for the push case too, but that's physically impossible: CI runs against a commit that must already exist, so a push can't be blocked by its own not-yet-run check — only a PR merge can be blocked pre-landing. What remains on the direct-push path is exactly what the plan calls detective, not preventive: §3.5 rule 3 (illegal-actor events "ignored and flagged" after fold) and V4 ("flag-only... never blocks git"). This directly undercuts the mitigation the plan itself claims for T-5 ("compromised partner system... single-writer sections localize damage") — T-5 is explicitly in-scope for v1 ("private circle of trusted teams," §10 header), not deferred to §10.6's public-mode checklist (which addresses a different problem: untrusted external parties, not a misbehaving v1 member). Test coverage matches the auditor's claim: CC-060 is "write PR into foreign section" and AC-101.2 is "when a member PRs into a foreign section, then merge is blocked" — both PR-path only; no CC/AC exercises a direct-push cross-section attempt, so the hole ships green. Recommendation (b), GitHub push rulesets with file-path restrictions, is a real, currently available GitHub feature and a workable fix; nothing in §17 or elsewhere in the plan acknowledges or defers this gap. Severity critical is justified: §10.1 names contract/lifecycle-state integrity as a primary asset in a YMYL chain, and the broken control is the plan's stated primary enforcement mechanism for cross-section write isolation.

## [critical/better] 05-schemas.md §5.4 (with 07-client.md §7.4, 08-agent-protocol.md §8.2 step 7, CC-081)
CLAIM: The registered-consumer registry lives partly outside the SSOT: consumers register by declaring dependencies in their private project's .a2a/config.yaml, but the validator that must block retire-before-acks (CC-081, AC-202.2) runs against the space repo and cannot see any project's private config.
WHY: The flagship safety feature (no breaking change ships without consumer awareness; retire blocked until acks) is unimplementable as written: V2 runs on the owner's machine and V3 in space CI — neither has access to other teams' project repos. Implementers will either silently drop config-declared consumers from enforcement (silent breakage returns) or invent an ad-hoc side channel. This also violates the plan's own D-001 single-authority principle: membership of the consumer set is normative data with no git-space home.
REC: Move consumer registration into the space: a schema'd declaration file in the consumer's own section (e.g. <system>/consumes.yaml listing contract id@major), written by `a2a` when a dependency is added (8.2 step 7 targets it instead of local config). Local config becomes a cache/mirror. Update 5.4's registered-consumer definition, CC-081/082, and AC-202.2 accordingly.
VERIFY-NOTE: Confirmed against primary text. §5.4 (05-schemas.md:96-97) defines registered consumer as EITHER a satisfied requirement (space-repo artifact, fully SSOT-visible) OR "declared dependency (in its project config, §7.4)". §7.4 (07-client.md:61-70) and §4.3 (04-topology.md:72-73, "Its local config (in the project, §7.4) lists connected spaces") make clear this config lives in each team's own private project repo, outside any exchange space — it has to, since one config lists which of N spaces to connect to. §4.1's authority map (04-topology.md:11-18), explicitly normative for every data class, has no row for this data: it isn't the space repo, isn't space.yaml, isn't hub-derived — it sits outside the whole system D-001 describes. §5.5's validation matrix confirms the two enforcement points that would need this data can't get it: V2 runs pre-write on the submitter's own machine, V3 is space-repo CI (space repo per 04-topology.md:67 contains only exchange content + manifest, never project repos) — neither has any path into another team's private codebase. §6.1/6.3 (06-hub-service.md) confirm the hub only mirrors "connected spaces" via git fetch on the space repo, never project repos, so even hub-derived views (dashboard's "registered consumers" column, 11-observability.md:17) can only ever reflect the requirement-based half. 08-agent-protocol.md §8.2 step 7 frames config-registration as THE explicit consumer-registration action ("Register any consumed contract in your project config dependencies (7.4) — this is what makes you a 'registered consumer'") — presented as the normal/primary path, not an edge case, and it's exactly the path taken whenever a consumer starts using an already-published contract without filing a requirement artifact (the common case, since requirements are for capabilities that don't yet exist). CC-081/AC-202.2's retire-block therefore cannot see roughly half of its own input set as specified. No other section (03, 06, 09, 12, 13, 14, 17) resolves or even flags this. The auditor's proposed fix (move registration into a schema'd space-repo file, e.g. <system>/consumes.yaml, written by `a2a` instead of local config) is consistent with D-001's single-authority principle and closes the gap cleanly.</note>
</invoke>


## [major/better] 03-domain.md §3.5 (fold rule 1; CC-012, CC-022; 06-hub-service.md OP-109)
CLAIM: Authoritative event ordering by client-minted ULID means the total order is derived from each writer's wallclock — clock skew reorders cross-party races arbitrarily, a misconfigured or malicious participant wins every race by backdating, and a late-arriving backdated event retroactively rewrites folded state (non-monotonic folds), which also breaks OP-109's watermark semantics (watermarks over ULID order vs. events arriving in git ingest order).
WHY: CC-022 says 'ULID order wins' for accept-vs-decline races — under skew the outcome is arbitrary and undetectable; event-sourcing practice (Kafka, EventStore) never trusts producer timestamps for log order. A consumer that processed up to watermark W will never see a newly-fetched event whose ULID sorts before W unless watermarks are redefined over ingest order — an unstated but load-bearing distinction implementers will get wrong.
REC: Use an order the SSOT already provides: first-parent commit order on main as the primary sort (tamper-evident, skew-immune), ULID only as tie-break within one commit; define OP-109 watermarks explicitly over hub ingest sequence, not ULID order; add a CC for backdated-ULID insertion with expected flag behavior.
VERIFY-NOTE: Confirmed. 03-domain.md §3.5 rule 1 makes ULID (client-minted, wallclock-derived) the sole fold-order key, and this is stated as authoritative even under known clock skew and out-of-order delivery — CC-012 ("fold order unaffected (ULID event order is authoritative)") and, decisively, CC-041 ("duplicate/out-of-order webhooks → ... fold by ULID") commit to ULID order with no commit-order/ingest-order fallback anywhere in the plan. §3.4/§7 ("a2a sync ... refresh local cache/fold") confirm the fold is a pure recompute over the full current event set, not an incrementally-locked state machine, so a late-arriving event with an earlier ULID genuinely re-sorts into the middle of history and can change the folded output (non-monotonic fold) — this is designed behavior, not an implementation slip.

The sharpest concrete failure isn't CC-022's accept/decline race (that's intra-system, near-simultaneous, and explicitly owned: "ULID order wins; loser flagged; visible on thread"). It's an XD decision (§3.4.4): two required approvers reach quorum → state becomes `approved` (binding, gates real consequences per G3/§3.7) → a party later submits a reject event whose ULID (via accidental clock skew or deliberate backdating) sorts before the quorum-completing approve → re-fold yields `rejected` and both approvals become illegal-transition flags, silently reversing an already-settled binding decision. No CC entry, no §10 threat (T-1..T-9), and no §10.6 public-mode checklist item covers this — signed events (the roadmap fix) authenticate an event but don't validate its embedded timestamp against causal reality, so the gap survives even the public-mode plan. The threat doesn't require malice: an honestly misconfigured clock produces the same rewrite, which is in-scope even for v1's trusted two-system circle, not just deferred "public mode" adversaries.

One sub-claim in the finding is weaker than stated: the OP-109 watermark-vs-ULID-order conflict. 06-hub-service.md §6.5 already separates the concepts ("watermarks" for position vs. "duplicate suppression by event ULID at the receiver side"), and 00-meta.md defines watermark as "a per-consumer marker of the last processed event" — textbook ingest-cursor semantics, not a ULID sort key. The plan never actually states watermarks are ordered by ULID, so this is better characterized as an unstated-but-probably-already-intended distinction that should be made explicit, not a proven break — real, but the weakest plank of the four cited.

Recommendation (first-parent commit order primary, ULID tie-break within a commit; explicit ingest-sequence watermark definition; new CC for backdated/skewed reordering of a settled state) is sound and directly closes the gap. Severity major is justified given the blast radius (a G3-gated binding decision, the highest-consequence exchange type in the taxonomy, can flip after settlement) and the plan's own "correct foundation for open source later" bar, where this becomes a real adversarial vector.

## [major/better] 05-schemas.md §5.4 (with 03-domain.md §3.4.1 G2, CC-080)
CLAIM: The breaking-change detector — the trigger for the G2 gate and the defense in CC-080 — is never specified: 'CI compat check' names no mechanism, and CC-080's backstop ('consumers' fixtures failing against new schema') appears in no V-point or §13 tier.
WHY: Schema registries treat the compatibility checker as the core artifact (Confluent Schema Registry compat modes, Buf breaking-change rules) precisely because humans and agents misclassify changes; without a specified mechanism, implementers ship semver-on-honor-system and CC-080 (breaking-as-minor) becomes the md-era silent drift the product exists to kill.
REC: Specify a concrete V3 rule for json-schema contracts: on minor/patch bump, all prior-version valid fixtures MUST still validate against the new schema (backward-compat smoke); on any fixture failure, require major. For openapi/proto formats state explicitly that compat is owner-CI duty and V3 checks only the version-vs-fixtures invariant. Name the check in §13 (T1 or T6) so AC-202.1 is testable.
VERIFY-NOTE: Confirmed. Traced all three cross-referenced anchors and none specifies a mechanism:

1. [03-domain.md:194](plan/03-domain.md#L194) — G2 gate row says "PR review + CI compat check" but names no algorithm.
2. [05-schemas.md §5.4](plan/05-schemas.md#L86-L102) — defines major/minor/patch classification semantically (what counts as breaking) and the gate/deprecation consequences, but never says how a check computes whether a given diff IS breaking. §5.3 ([05-schemas.md:73-76](plan/05-schemas.md#L73-L76)) says for `json-schema-2020-12` "the binary validates fixtures against schemas itself" — but that's validating a schema's OWN fixtures (goldens), not diffing old-version-fixtures against new-version-schema for backward compat.
3. [12-corner-cases.md CC-080](plan/12-corner-cases.md#L82) explicitly concedes "V3 cannot always prove semantics" and posits "consumers' fixtures failing against new schema" as the backstop, citing "(13)" — but [13-testing.md T1](plan/13-testing.md#L10) is per-current-schema golden fixtures (valid+invalid for THAT schema), not a cross-version compat check; the V1–V5 matrix in [05-schemas.md §5.5](plan/05-schemas.md#L104-L120) has no row for it either. So the cross-reference is dangling exactly as claimed.

Additional corroborating gap the auditor didn't cite but that strengthens the finding: [04-topology.md §4.2](plan/04-topology.md#L34-L39) puts `fixtures/` only inside `provides/<contract-slug>/`, owned solely by the providing system — there is no structural home in the layout for "consumers' fixtures" at all, so CC-080's backstop references an artifact that doesn't exist anywhere in the topology. And [07-client.md OP-212](plan/07-client.md#L39) ("`a2a contract new/publish/...` opens PR when G1/G2 applies") gives no indication the binary independently detects breaking-ness rather than trusting the author's own semver bump — i.e. the gate trigger plausibly reduces to string-parsing the version field, which is precisely the honor-system risk described.

AC-202.1/E2E-4 ([14-us-ac.md:41-43](plan/14-us-ac.md#L41-L43), [13-testing.md:39](plan/13-testing.md#L39)) only test the already-correctly-labeled-major path, giving zero test coverage to CC-080's actual scenario (mislabeled minor). The recommendation (differentiate json-schema built-in fixture-based compat rule vs. openapi/proto owner-CI duty, and name the check in a §13 tier) is a proportionate, non-overengineered fix given G2 is 1 of only 5 human gates and is the core anti-drift mechanism the product exists to provide.

## [major/better] 02-actors.md §2.3 (with 09-human-ops.md §9.2, 10-security.md §10.3)
CLAIM: space.yaml is declared the membership SSOT, but the actual enforcement state (GitHub teams, CODEOWNERS, branch protection/rulesets, PAT scopes) is hand-configured with no specified reconciliation or drift detection between manifest and host settings.
WHY: This is the GitOps lesson: declared state without a reconciler silently diverges from actual state. Concrete failure: participant flipped to `left` in the manifest but their team/PAT not revoked — manifest says read-only, host still grants write; or a new section scaffolded without its CODEOWNERS entry — writes land unprotected. §12 has no CC for manifest/host drift, so no test will catch it either.
REC: Add a specified check (a2a doctor --space or a hub V4-class check) that diffs space.yaml against actual host configuration (teams, CODEOWNERS entries, protection rules, collaborator list) and flags divergence; add the corresponding CC and AC. Full auto-reconciliation (safe-settings-style) can stay post-v1, but detection must be v1.
VERIFY-NOTE: Confirmed by direct reading, not just absence-of-mention. 02-actors.md §2.3 makes space.yaml "the SSOT of membership" including status (active/suspended/left) and credential scopes, but the only offboarding mechanism (09-human-ops.md §9.2: "manifest status → left, revoke credentials (10.5), CODEOWNERS entry removed") is three separate manual operator steps with nothing that verifies they were all actually applied, or re-verifies later that GitHub state still matches. 10-security.md §10.3's "dual enforcement principle" (git-mechanics + V3/V4 content re-check) is real but narrower than it sounds: CC-002/V2/V3 reject `from ≠ owning section` (wrong-section writes), which does mitigate the "new section scaffolded without CODEOWNERS" half of the auditor's scenario IF branch protection actually requires V3/CI to pass before merge — but nothing checks that requirement itself is configured. Critically, no rule anywhere ties a write's validity to the writer's manifest `status`: I grep'd every file for "suspended"/status-gated write logic and found none — CC-008/AC-102.2 only reject `to:` referencing a `left` system (recipient side), never `from` a `left` system whose GitHub credential wasn't actually revoked (author side). CC-061 assumes revocation already happened ("revoked credential mid-session"); CC-062 only covers exchange-level fallout, not host-state verification. `a2a doctor` (07-client.md OP-218) is explicitly a self/local diagnostic ("credentials, space access, versions, CI presence, statusline wiring" for the running participant), not a space-wide audit comparing manifest to GitHub teams/CODEOWNERS/branch-protection/collaborator list. The hub's read-only API surface (06-hub-service.md §6.2, OP-101..111) has no membership/host-config-diff endpoint, and F-1..F-9 failure modes don't include this class. 17-decisions.md's open-questions list (Q-001..Q-008) doesn't carry it either — this isn't a consciously deferred item, it's simply absent. So the auditor's core claim holds: there is no specified check/CC/AC that diffs space.yaml against actual host enforcement state, and the specific failure scenario (left-but-not-revoked participant retains write access; unprotected new section) is real and only partially mitigated by existing content-level validation.

## [major/better] 10-security.md §10.7 (with CC-045, T-4)
CLAIM: There is no redaction path: the plan mandates 'reverts, not history rewrites' and alarms force-pushes, so any secret, private code, or end-user PII that slips past V2/V3 (scan miss or G5 override) remains permanently readable by every space member, hub mirror, and local clone.
WHY: Secret-scan heuristics have known false-negative rates; T-4 is a named top threat. Rotation fixes credentials but not leaked private code or PII (where GDPR erasure applies — spaces are shared across orgs). The only remedy the plan offers (git revert) does not remove content from history, and the only mechanism that could (coordinated rewrite) is classified as an anomaly to be alarmed.
REC: Specify an operator-coordinated redaction runbook in §9/§10.7: agreed history rewrite of the affected space, participant re-clone step, hub full re-index (F-7 machinery already exists), local cache purge, and an announcement artifact recording the redaction. Add a CC distinguishing sanctioned redaction from hostile force-push.
VERIFY-NOTE: Verified against plan text. §10.7 states verbatim: rotate credentials, `git revert` offending commits, "the space stays append-only — reverts, not history rewrites; force-push is alarmed by F-7" (10-security.md:92-94). CC-045 (12-corner-cases.md:52) and F-7 (06-hub-service.md:102) confirm force-push/history rewrite is treated purely as an anomaly triggering full re-index + operator alert, with no sanctioned/hostile distinction. §3.8 independently states "Archival: artifacts never move or get deleted; git history + closed states are the archive" (03-domain.md:216), and D-017 codifies lifecycle events as append-only-forever — immutability is a deliberate, repeated design commitment, not an oversight, which makes the missing remedy path more load-bearing, not less. CC-010/T-4 mitigation (10.4) is purely preventive (V2/V3 scan + G5 human override at write time); nothing in 09-human-ops.md (credential lifecycle, day-2 ops, succession) or 17-decisions.md (Q-001..Q-008 open questions) covers post-hoc removal of leaked content — Q-001 is about per-artifact encryption for `restricted`, not remediation of an already-committed leak. No mention of GDPR/PII/redaction/purge anywhere in the 20 files (grep confirms only the single forbidden-payload-class mention at 10.4). So: git revert leaves the blob in history, readable by every space member/hub mirror/local clone indefinitely; the only mechanism that could remove it (coordinated rewrite) is explicitly the thing the plan alarms on with no carve-out for sanctioned operator-run redaction. The recommendation is correctly grounded too: F-7's full-reindex machinery already exists and could be repurposed for a redaction runbook, and no CC currently distinguishes sanctioned redaction from hostile force-push. Finding is CONFIRMED — severity major is reasonable given the operator's stated bar of "correct foundation for open source later" (cross-org PII/secret exposure with no removal path is exactly the kind of gap that bites once spaces are shared beyond a trusted two-party circle).</note>
</invoke>


## [major/better] A2-examples.md B.6/B.7/B.8 (vs 03-domain.md §3.3, 05-schemas.md §5.2, 16-handoff-directive.md §16.2)
CLAIM: The golden fixtures — declared normative and literally the T1 valid set (§13.5) — contradict the normative rules: B.6's ID `XD-getvisa-…` uses the space, not the authoring system, violating §3.3; B.6's `from: axon` with placement in shared `decisions/` violates §5.2's 'from MUST match owning section' (the decisions/ exception is never specified as a validator rule); B.7's ref `XC-…@2.2.0#sha256:…` uses a combined version+digest form absent from §5.2's ref grammar; B.8's `origin` holds an exchange ID while §5.2 defines origin as opaque *local tracker* IDs (§16.2 redefines it as originating exchange IDs).
WHY: Implementers are told fixtures are normative goldens and that conflicts are plan defects (§0.1) — they will build a validator that either rejects its own golden set or silently widens the grammar to make fixtures pass, and the origin field ships with two incompatible meanings across types.
REC: Fix all four: re-mint B.6's ID per §3.3 (or explicitly define space-authored IDs for decisions plus the decisions/ section-ownership exception in §5.2); either add `id@version#digest` to the ref grammar or correct B.7; split origin into `origin` (local tracker, per §5.2) and reuse `refs`/a dedicated `in_response_to` for originating exchanges in §16.2.
VERIFY-NOTE: Hinge: §0.1 line16 states fixtures 'are normative as golden fixtures'; §13.5 confirms Appendix B examples 'are literally T1 fixtures (golden valid set)'; §0.1's precedence rule requires conflicts to be reported as plan defects, not resolved silently. So a fixture-vs-spec mismatch is a defect by construction. All four sub-claims verified against text: (1) B.6 id `XD-getvisa-20260802-r1w9` — §3.3 says decision (class X per §3.1) IDs take `<PREFIX>-<system>-...` where `<system>` is 'the authoring system'; systems are axon/seomatrix per §0.5, not 'getvisa' (the space id, §0.4/§4.2). B.6 also contradicts its own `from: axon` field. (2) B.6 `from: axon` placed under `decisions/` — §5.2 states baldly 'from MUST match owning section (validator + authz)' with no carve-out; §4.2's 'writes ONLY under its own section (plus decisions/ via the decision flow)' is a CODEOWNERS/write-authz statement (confirmed against §10.3 AuthZ matrix, which has no decisions-specific row), not a redefinition of the §5.2 validator rule, and decisions/ per §4.2 has CODEOWNERS = all required parties, i.e. no single owning section for the validator to match against. (3) B.7's second ref `XC-seomatrix-content-payloads@2.2.0#sha256:9e1a…` combines version+digest; §5.2's grammar is exactly `id@version | id#digest | space:id...` (confirmed also in §3.8, §5.7) — no combined form exists, and it isn't the cross-space `space:id` form either. (4) B.8's `origin: [XW-seomatrix-20260722-a1a1]` matches §16.2's handoff-extension definition ('originating exchange/requirement IDs') but directly contradicts §5.2's common-base definition ('opaque local tracker IDs (epic/spec); never file paths'); §5.2.1's per-type extensions table does not list `origin` among handoff's extra fields, so §16.2 silently redefines a base-envelope field rather than legally extending it. All four are genuine, unresolved textual contradictions; none is addressed or reconciled elsewhere in the 20 files read (03,04,05,10,13,16,17,00-meta,00-STRUCTURE, A2).

## [critical/simpler] 05-schemas.md §5.4 + 07-client.md §7.4 + 08-agent-protocol.md §8.2.7
CLAIM: The retire/deprecation safety mechanism depends on 'registered consumers' derived partly from each project's private `.a2a/config.yaml`, which the space CI validator (V3) and hub can never see.
WHY: V3 runs inside the space repo and the hub mirrors only spaces; declared dependencies live in each participating project's own repo. AC-202.2 / CC-081 ('validator blocks retire until all registered consumers acked') is unimplementable for config-declared consumers — the flagship YMYL safety rule (D-010) silently misses exactly the consumers §8.2 step 7 tells agents to register.
REC: Make consumer registration a committed artifact in the consumer's own section of the space (e.g. a small `consumes/` declaration file, or reuse satisfied requirements as the only registration channel) and drop the project-config channel from the registered-consumer definition. One SSOT, visible to V3/V4.
VERIFY-NOTE: Confirmed. [05-schemas.md §5.4](plan/05-schemas.md#L96-L97) defines registered consumer disjunctively: "any system whose satisfied requirement OR declared dependency (in its project config, §7.4) references the contract." The second disjunct is unenforceable by any validator in the system:

1. [07-client.md §7.4](plan/07-client.md#L63-L64): `.a2a/config.yaml` is "Per participating project: a small committed config" — i.e. it lives in the consuming system's own private project repo, not in the exchange space repo.
2. [04-topology.md §4.3](plan/04-topology.md#L72-L75) confirms the separation explicitly: "Its local config (in the project, §7.4) lists connected spaces... the hub aggregates all spaces it is granted read access to" — the hub's read scope is spaces, never arbitrary project repos.
3. [05-schemas.md §5.5](plan/05-schemas.md#L113) (V3): "CI on space repo | every push/PR | full-repo" — V3 is scoped to the one space repo it runs in; it has no visibility into any other system's project repo.
4. No sync/publish path exists anywhere in the plan that copies project-config dependency declarations into the space or into the hub — grepped all 20 files, the only two mentions of "declared dependency"/project config in this context are the 5.4 definition itself and the 8.2 instruction to populate it.

[08-agent-protocol.md §8.2 step 7](plan/08-agent-protocol.md#L43-L45) explicitly instructs agents to register via exactly this channel: "Register any consumed contract in your project config dependencies (7.4) — this is what makes you a 'registered consumer'." So the protocol steers registrations into a channel the enforcement mechanism cannot see.

CC-081/CC-082 ([12-corner-cases.md](plan/12-corner-cases.md#L83-L84)) and AC-202.2 ([14-us-ac.md](plan/14-us-ac.md#L44-L45)) assert "validator blocks retire until all registered consumers acked" without qualification or carve-out — the plan does not anywhere acknowledge that only the satisfied-requirement half of the definition (which lives inside the space, under the consumer's own `requires/` section, and so IS visible to V2/V3/hub) is actually enforceable. The config-declared half is a silent, unimplementable gap in D-010's flagship YMYL safety rule. Auditor's fix (make registration a committed artifact inside the space, e.g. reuse the satisfied-requirement channel and drop the project-config channel from the definition) directly closes the gap and matches the one-SSOT principle the plan applies everywhere else (D-001 in 04-topology.md).</note>
</invoke>


## [major/simpler] 03-domain.md §3.4.3/§3.4.6 + 12-corner-cases.md CC-029 + 07-client.md OP-211
CLAIM: Closure semantics are contradictory: §3.4.3 says `verify` transitions responded→closed, §8.2 says 'a2a verify (closes)', yet CC-029 requires a separate 'sender's close event' after per-response verification, and OP-211 lists both `verify` and `close` verbs with no distinction.
WHY: The fold is the semantic core implemented once and reused by binary and hub; two readings (verify-closes vs verify-per-response + explicit close) produce different state machines. An implementer will pick one and half the plan's own references will be wrong.
REC: Define exactly one rule in §3.4.3/3.4.6 — e.g. `verify` targets a response; the parent closes when the sender verifies with no outstanding expected work, or via an explicit `close` only in the multi-response case — and remove or define the redundant verb in OP-211.
VERIFY-NOTE: Confirmed. §3.4.3 (03-domain.md:106) defines a single unconditional transition for question/work_request/proposal: responded --verify--> closed, actor=sender, with no notion of partial/per-response verification. §3.4.6 (03-domain.md:133-138) says a response is individually verifiable and 'the parent closes only when the sender's verify event says so' — already ambiguous about which verify event does the closing. CC-029 (12-corner-cases.md:38) then explicitly requires a *separate* 'sender's close event' distinct from per-response verification events, directly contradicting 3.4.3's model where `verify` itself IS the closing transition (there is no `close` row anywhere in 3.4.3's table). OP-211 (07-client.md:38) lists `verify` and `close` as two separate lifecycle verbs, reinforcing the CC-029 reading — but `close` as a transition/event name appears in NO normative transition table in 03-domain.md, so the CLI surface references an undefined state-machine event. Meanwhile §8.2 (08-agent-protocol.md:39-40, 'Pass → `a2a verify` (closes)') reinforces the opposite, single-response reading where verify and closing are literally the same act. I also checked A2-examples.md's only worked 'verify (closes parent)' example (line 277, B.11): it's actually for a **requirement** artifact using the `satisfy` transition (§3.4.2), not a question/work_request/proposal `verify` transition — so no worked example anywhere resolves the CC-029 multi-response case. There's also no schema field (checked 05-schemas.md) tracking 'how many responses expected'/'final response' that would let the fold algorithmically decide when a verify closes vs. doesn't — the mechanism is genuinely undefined. Given §3.5 states the fold is 'implemented once in the binary, reused by hub' and must be deterministic, this ambiguity (verify-always-closes per 3.4.3/§8.2 vs. verify-per-response-plus-separate-undefined-close-event per 3.4.6/CC-029/OP-211) is a real, unreconciled contradiction that would force an implementer to guess — the finding accurately describes both the contradiction and its severity (major, since it's the core fold semantics reused everywhere).

## [major/simpler] 08-agent-protocol.md §8.7/§8.8 + 17-decisions.md D-015
CLAIM: Requiring skill/rule texts to be 'generated projections of this plan and the binary reference', drift-gated in CI, specifies a prose-generation pipeline that is ill-defined and heavy for v1.
WHY: Unlike templates-from-schemas, 'generate the 8.1–8.6 loops as prose' has no deterministic generator; an implementer must either build an LLM release step with review or fake the gate. Either way it's meta-machinery serving 2 teams, and D-015 makes it normative.
REC: Hand-maintain the skill/rule text as versioned files in the product repo with a release-checklist review step; keep automated drift gates only for the mechanically derivable parts (command/MCP reference from the binary, templates from schemas). Soften D-015 to 'single-sourced in the product repo, released together'.
VERIFY-NOTE: Confirmed. §8.7's "generated from this plan and the binary's reference at release time" bundles two very different content classes without distinguishing them: (a) mechanically derivable material (full command/MCP reference — extractable from the binary, like the 8.1–8.6 loops themselves, which are already terse prose that can be near-verbatim transcluded), and (b) genuinely synthesized material — "condensed §0/§3 semantics," "troubleshooting (`a2a doctor` interpretations)," and "onboarding walkthroughs (§9 digests)" (§8.7). "Condensed" and "digest" name a summarization act, not a rendering of structured data — no template, extractor, or algorithm for producing them from plan prose is specified anywhere in the 20 files. Contrast with the plan's other no-drift constructions, which ARE mechanical: §5.3 (CI regenerates contract templates from schemas) and §13.5 (Appendix B examples ARE the T1 fixtures — zero drift by file identity). No such construction exists for skill/rule prose.

This gap is not open/deferred — it is normatively closed shut by the plan itself: D-015 says "*all* harness texts are generated projections of this plan and the binary reference, drift-gated"; §8.8 states "no hand-maintained copies — R-004 applies to prose too"; AC-602.1 (14-us-ac.md) puts it behind a hard CI gate: "Given the release pipeline, when plan/binary reference change, then the skill text is regenerated or the build fails (drift gate)." The plan explicitly forecloses the cheap workaround (hand-maintaining the skill as source), leaving an implementer only the two options the auditor names: build an LLM-based generation+review step, or fake/skip the gate. Neither is designed or budgeted anywhere in the plan (09-human-ops.md §9 shows the same unspecified pattern for runbooks: "final runbooks ship as product docs generated at release," no mechanism). The A1 mate-amendments file confirms the artifacts are vendored at a pinned version ("a2ahub generates them, D-015") without saying how. This is a real specification gap for a v1-scoped, 2-team, no-overengineering plan, and the auditor's recommendation (hand-maintain skill/rule text with a release-checklist review, keep automated drift gates only for the mechanically derivable parts, soften D-015) is a proportionate fix.</note>
</invoke>


## [major/simpler] 06-hub-service.md §6.2 OP-109 + 00-meta.md glossary 'watermark' + 04-topology.md §4.1
CLAIM: OP-109 (events-since-watermark API) and the public per-consumer watermark concept have no consumer anywhere in the plan.
WHY: The statusline is pull-based current-state (OP-108), the binary syncs via git, and chat fan-out needs only a hub-internal cursor. A public at-least-once event-delivery API with per-consumer watermarks is event-bus machinery built on spec, not need — classic speculative generality.
REC: Cut OP-109 and the glossary/4.1 'watermark' concept from v1; keep an internal delivery cursor for the chat channel as an implementation detail of §6.5. Reintroduce a consumer API when a real consumer appears (R-012 says new channels can be added without migration anyway).
VERIFY-NOTE: Verified by exhaustive grep across all 20 plan files for "OP-109", "watermark", "events since", "consumer ID", "per-consumer": OP-109 appears only in its own definition (06-hub-service.md §6.2: consumer ID + watermark in, ordered events + next watermark out) and in the glossary (00-meta.md) / index (00-STRUCTURE.md). No client, dashboard, statusline, or channel spec anywhere calls or pulls from it. Actual consumers of hub data, confirmed by grep: statusline is OP-108 pull (06-hub-service.md §6.5, 07-client.md:80, 08-agent-protocol.md:96); dashboard is fed by "OP-106/107/108/110 + SSE for live updates" (11-observability.md:10), and the SSE stream explicitly "mirrors OP-106/OP-108 updates" (06-hub-service.md:35) -- not OP-109; chat webhook is hub-push ("push: generic JSON POST", §6.5) driven by hub.yaml routes, not a client pulling via OP-109. §6.5's "per-channel watermarks" and F-9's "watermark holds; redelivery on recovery" describe an internal delivery-tracking mechanism for the push channel -- textually distinct from OP-109's public per-consumer-ID pull API -- so the internal cursor the finding says to keep is already separable from OP-109 as written. 4.1's topology row ("watermarks, notification status, subscriptions... hub... safe to lose") is generic delivery-state bookkeeping, not evidence of an OP-109 caller. R-012 (01-vision.md:119, "extensible foundation... new channels... without migrations") is real and backs the recommendation that a consumer API can be reintroduced later at zero cost. No other file (07-client, 08-agent-protocol, 09-human-ops, 10-security, 12-corner-cases, 13-testing, 14-us-ac, 16-handoff-directive, 17-decisions, A1, A2) mentions OP-109 or any events-since-watermark caller. The finding accurately identifies OP-109 plus the public per-consumer watermark framing as unconsumed speculative machinery; the recommended fix (cut OP-109, keep an internal delivery cursor as a §6.5 implementation detail) matches how §6.5's chat-channel watermark is already phrased.

## [major/simpler] 03-domain.md §3.1 + 05-schemas.md §5.2.1
CLAIM: Two of the ten types are redundant: `proposal` is a `work_request` with category `contract-change|process-change` (same §3.4.3 lifecycle already), and `status_report` is an `announcement` with category `status` plus a `period` field.
WHY: Each type costs a schema, a template, golden fixtures (valid+invalid), matrix cells in T1/T3/T5, per-type authoring guides in the skill, and taxonomy-classification burden on every sending agent. `proposal` and `status_report` add zero lifecycle behavior for that price; 8 types close the same pain.
REC: Fold `proposal` into work_request categories and `status_report` into announcement categories (amend D-004 with the operator). If the operator insists on keeping the names, at minimum make them schema aliases of the merged types so the implementation surface is 8, not 10.
VERIFY-NOTE: Confirmed against the primary text, not just the summary table.

- `proposal`: §3.4.3's own header is "question / work_request / proposal (exchange)" — one shared transition table for all three [03-domain.md:94]. §5.2.1 shows proposal's only delta from work_request is a required `proposed_change` field + a refs-target requirement and its own category enum `contract-change|process-change|other` [05-schemas.md:58] — i.e. exactly the shape of a category split, same pattern work_request already uses for data|feature|fix|investigation.
- `status_report`: §3.4.7's header is literally "announcement / status_report (broadcast)" — one shared lifecycle description, `draft → publish → published`, immutable, no ack required [03-domain.md:140-146]. Its only schema delta is an optional `period?` field; it has no category enum at all [05-schemas.md:63], whereas announcement's enum (`release|deprecation|migration|incident|notice`) could trivially take a `status` value.
- Corroborating precedent inside the plan itself: the pre-interview draft taxonomy [00-STRUCTURE.md:81] listed `data_request` and `contract_change_proposal` as separate candidate types; D-004 explicitly folded `data_request` into `work_request` category `data` [17-decisions.md:15, 03-domain.md:28-29] but did not apply the same fold to `contract_change_proposal` (→ `proposal`) despite the same reasoning applying — an unexplained asymmetry that supports the finding.
- Grep for `proposal`/`status_report` across 02, 06, 11, 12, 14 (actors, hub-service, observability, corner-cases, US/AC) returns zero hits — no downstream component branches on these type tags specifically, i.e. no lifecycle behavior would be lost by folding.
- Cost claim is substantiated: §13.2's coverage matrix is explicitly generated from "object type (10) × lifecycle path × actor role × surface" [13-testing.md:20-23], so the type count is a literal multiplier on T1/T3/T5 volume, and §8.2's send-loop classify step already forces agents to pick between `work_request` ("work/data") and `proposal` ("change to their artifact") — a real, admitted classification burden [08-agent-protocol.md:25].
- §3.1's "Responds" column difference (work_request "yes" vs proposal "yes (accept/decline)") is cosmetic — both resolve through the identical accept/decline/respond/verify events in 3.4.3, so it doesn't rebut the merge.

D-004 is origin `interview` (operator-decided), which matches the finding's own recommendation to route the fix back through the operator rather than unilaterally amend. Nothing found in the plan defends the ten-count as deliberate or shows behavior unique to these two types, so the "already handled elsewhere" escape hatch doesn't apply.</note>


## [major/simpler] 03-domain.md §3.4.1 + 05-schemas.md §5.2.1/§5.7
CLAIM: Standing-artifact version mechanics are unspecified: the contract file is edited in place per version yet consumers reference `id@version` as immutable content, and no mechanism (per-version files, tags, version→commit map) is defined for resolving past versions; the `stability: draft|stable` field additionally duplicates the folded lifecycle state.
WHY: Resolving `XC-axon-ingest@1.0.0` after 2.0.0 lands requires git-history archaeology the binary/hub must implement — a nontrivial invisible feature; and `stability` creates a second truth next to the event-folded draft/published state, violating the plan's own two-truths rule.
REC: Specify the simplest resolution rule explicitly (e.g. publish event records the commit SHA of the version, or immutable `versions/<semver>/` snapshots) and delete `stability` in favor of folded state.
VERIFY-NOTE: Confirmed. Storage layout [04-topology.md §4.2] gives exactly one mutable file per contract: `provides/<contract-slug>/contract.md` (+ single `schema/`, `fixtures/`) — no `versions/<semver>/` subtree, no per-version file, no git-tag scheme anywhere in the plan (checked 03-domain, 04-topology, 05-schemas, 06-hub-service, 07-client, 08-agent-protocol, 12-corner-cases, 17-decisions). Yet §3.4.1 (03-domain.md:79-80) asserts 'Every published version is immutable (git + digest). Consumers reference id@version', §5.4 (05-schemas.md:98) requires 'Old versions remain published until retire' — i.e. two+ versions must be simultaneously resolvable during the mandatory deprecation/sunset window — and §8.4 (08-agent-protocol.md:70-71) confirms the actual mechanic is 'commit contract + fixtures together' on the same path each version bump, i.e. in-place overwrite. No section defines how `a2a show XC-axon-ingest@1.0.0` resolves after 2.0.0 has overwritten contract.md — not even a stated 'walk git log' convention. This is worse than 'git archaeology is undocumented': the single-file-per-slug layout can't even hold two simultaneously-published versions at once, contradicting §5.4's own coexistence requirement. Real, and major severity is justified since id@version pinning is load-bearing across refs/satisfy/response-verification (05-schemas.md:44, 03-domain.md:89, 08-agent-protocol.md:65).

Secondary claim (stability duplicates folded state) also holds up, though more weakly: `stability: draft|stable` (05-schemas.md:54) is a manually-written frontmatter field, never explained anywhere in the plan (only 2 other mentions: an example and a dashboard column), whose enum value 'draft' collides in name with the fold-derived lifecycle state 'draft' (03-domain.md §3.4.1) that D-017 (17-decisions.md:28) explicitly designed the event-fold to replace ('no in-place status edits (two-truths)'). The example (A2-examples.md:26) only shows stability:stable co-occurring with a published contract, never demonstrating a scenario where it's genuinely orthogonal to fold state, so the redundancy concern is well-founded even if not airtight.

## [major/simpler] 16-handoff-directive.md §16.2 vs 05-schemas.md §5.2 (`origin`)
CLAIM: `origin` is defined as opaque LOCAL tracker IDs ('never paths', §5.2) but §16.2 redefines it as 'originating exchange/requirement IDs', and fixture B.8 uses an exchange ID — one field, two conflicting normative meanings.
WHY: Schema authors and fixture authors will implement different validation for the same field; A2 fixtures are normative goldens, so the conflict lands directly in T1.
REC: Keep `origin` for local tracker IDs only; make handoff lineage use `refs` (which already supports notes and pinning) or a dedicated `parent`/`fulfills` field like response has.
VERIFY-NOTE: Confirmed — actually a three-way conflict on one field name, not just two.

1) Base definition, stated twice: [05-schemas.md:45](plan/05-schemas.md#L45) `origin?` = "opaque local tracker IDs (epic/spec); never file paths", echoed verbatim in [03-domain.md:180](plan/03-domain.md#L180) "opaque local tracker IDs, e.g. mate epic/spec — never paths". Direction confirmed by [08-agent-protocol.md:58](plan/08-agent-protocol.md#L58): the RECEIVER records the a2ahub exchange's own ID into ITS OWN local tracker's origin-equivalent field — i.e. `origin` lives in the external/local system and never holds an a2ahub artifact ID.

2) [16-handoff-directive.md §16.2:39](plan/16-handoff-directive.md#L39) reuses the identical field name in the XH envelope-extension table and defines it as "originating exchange/requirement IDs" — a2ahub's own artifact IDs, the opposite of (1).

3) [A2-examples.md:206](plan/A2-examples.md#L206) fixture B.8 sets `origin: [XW-seomatrix-20260722-a1a1]` — an a2ahub XW (work_request) exchange ID — matching §16.2's meaning and directly violating §5.2/§3's "opaque local tracker IDs ... never file paths."

Bonus corroboration the auditor didn't even cite: [14-us-ac.md:149](plan/14-us-ac.md#L149) AC-1002.1 uses `origin` to point at a migrated artifact's "legacy path" — a third, mutually exclusive meaning that directly contradicts the explicit "never file paths" rule stated everywhere origin is normatively defined.

This is not a misreading or something already reconciled elsewhere: the same field name `origin` carries three incompatible normative meanings (external local-tracker ID / a2ahub exchange-or-requirement ID / legacy file path), and the A2 golden fixture — normative for T1 per the plan's own testing framework — instantiates the incompatible meaning. The auditor's fix (keep `origin` for local-tracker IDs; give handoff lineage its own field, mirroring response's existing `parent` field, [05-schemas.md:60](plan/05-schemas.md#L60)) is directly actionable and consistent with the schema's existing per-type pattern. Severity "major" is justified: an unresolved field-semantics conflict lands directly in schema authoring and T1 fixture validation, exactly as claimed.

## [critical/implementer-gaps] 05-schemas.md §5.3/§5.4 + 04-topology.md §4.2 + 03-domain.md §3.4.1
CLAIM: Storage and resolution of multiple concurrently-published contract versions is undefined: the layout has exactly one contract.md per slug with a single `version` envelope field, yet §5.4 requires old versions to 'remain published until retire' and consumers to pin `id@version`.
WHY: An implementer cannot build `a2a show XC-axon-ingest@1.0.0`, V5 digest checks of pinned refs, or per-version deprecate/retire (§3.4.1 'deprecate (version or whole)') without inventing a versioned storage scheme (directories per version? git tags? history walk?). Every choice affects layout, fold, validator, hub index and dashboard 'sunset countdowns' — a wrong guess here forces a space-layout migration later.
REC: Specify normatively: how a published version is materialized in the repo (e.g. `provides/<slug>/versions/<semver>/` snapshots vs git-tag resolution), how `id@version` and `id@version#digest` resolve, and how per-version lifecycle state (published/deprecated/retired) is represented and folded separately from whole-contract state.
VERIFY-NOTE: Confirmed against plan text. 04-topology.md §4.2 layout has exactly one `contract.md`/`schema/`/`fixtures/` per `<contract-slug>` — no versions/ subdirectory or per-version snapshot. 05-schemas.md §5.2.1 gives the contract type a single scalar `version` field (not a list/history). Yet 03-domain.md §3.4.1 requires the lifecycle to support 'publish (new version)' repeatedly, 'deprecate (version OR whole)' as two distinct scopes, and 05-schemas.md §5.4 mandates 'old versions remain published until retire' with `id@version` consumer pinning and per-version retire gated on 'zero registered consumers' — i.e. genuine per-version state, not just per-contract state. The event schema (§3.5) has no field to scope a transition to one version vs the whole contract (only `subject artifact ID` + free-text `refs`), and §3.4's fold model computes one state per artifact ID — so 'deprecate (version)' vs 'deprecate (whole)' collapse into the same folded state with no normative way to tell them apart. CC-030 (12-corner-cases.md) and the §11 dashboard 'sunset countdowns' both presuppose per-version deprecation tracking that the storage layout can't materialize as specified. Grepped the whole plan (topology, hub-service, testing, decisions, corner-cases, examples) for any versions/ directory, git-tag scheme, or id@version resolution algorithm — none exists; it is not listed as a deferred/open question in §17 either. The auditor's cited sections and cross-references are accurate, the contradiction is real, and the recommendation (normatively define version materialization + per-version lifecycle state) correctly targets the fix.

