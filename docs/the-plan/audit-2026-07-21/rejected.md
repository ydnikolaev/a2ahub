- [03-domain.md §3.3 / 04-topology.md §4.3 (CC-071)] System IDs and space IDs form a flat namespace whose uniqueness is guaranteed only by a single operator's fleet registry, and both are baked into immutable artifact IDs and cross-space refs — a single-operator assumption that a format change would be needed to undo. || REJECTED: Finding misreads what R-007/R-012 promise, and mischaracterizes what fixing this would require. R-007's "without format changes" attaches to the hub-mediated *write path* (authn/authz), not to identity/naming — confirmed verbatim by D-002 (highest-precedence section): "public mode gets server-side enforcement without format migration." R-012's no-migration promise is scoped to new object classes/channels. Neither requirement says anything about the space/system-ID scheme, so "this is the one place the format provably changes" invokes a guarantee the plan never made.

The descriptive premise is accurate — the namespace IS flat and fleet-scoped: §0.5 "System IDs are ... globally unique within all spaces they join"; §4.3 "Space IDs are globally unique within an operator's fleet (registry kept in the hub config)"; CC-071 enforces this only via an onboarding-time registry check, not a structural constraint. But this is a consciously-scoped, documented choice for a single-operator v1 (glossary: operator = "the human who administers the hub and owns the org hosting spaces. Currently Yura"), not a hidden trap.

The "format change would be needed to undo" claim also fails on mechanics: CC-004 already establishes artifact IDs are space-scoped and collisions ACROSS spaces are explicitly legal ("IDs are space-scoped; cross-space refs carry space ID"); the cross-space ref form `space-id:artifact-id@version` is resolved through indirection, not literal global lookup — §7.4's local config already keys connected spaces by repo URL + mirror path, meaning resolution is anchored to repo URL, not the bare id string. A future space-id collision (e.g. two merged operator fleets) would be fixed by extending that resolution layer with an alias/rename mapping — additive, resolution-side work — not by rewriting the `space-id:artifact-id@version` syntax or any already-committed immutable artifact. That is in fact close to the auditor's own proposed remedy ("a locally-configured alias mapped to it"), just implemented as a config-layer fix rather than a format rewrite.

Finally, the plan's actual stated "public mode" roadmap (§10.6, D-002, Q-003) is about hardening the existing single-operator hub for untrusted participants joining spaces it already hosts — not about federating independent hub deployments run by separate operators. No open question (Q-001..Q-008) tracks multi-operator namespace merging the way genuine forward risks are tracked (Q-002 scale, Q-003 signed identity), consistent with it being out of the plan's actual scope rather than an overlooked defect.

Verdict: the finding's description of the mechanism is correct but its diagnosis (major severity, format-breaking, R-007/R-012-violating) is not supported by the text — real=false.
- [13-testing.md §13.2] The generated combinatorial e2e grid (10 types × every lifecycle path × 3 roles × 2 surfaces × 5 orthogonal injections, 'each cell an executable scenario') is the single largest hidden build cost in the plan and duplicates coverage T2 already provides. || REJECTED: Финдинг не подтверждается — две ключевые предпосылки опровергаются текстом плана.

1) "Hidden cost" неверно: [15-rollout.md:44-51](plan/15-rollout.md#L44-L51) — Phase L4 явно называет 'full §13 matrix including E2E-3…10 and cc-coverage enforcement' отдельным именованным deliverable ПОСЛЕДНЕЙ фазы ('foundation complete... R-013 фундамент'), отдельно от pain-closing фаз L0-L3 (toolchain, day-one content, hub/signals — реальная боль уже закрыта в L2: 'the md-relay era ends'). Exit-критерий явный ('every CC mapped to a green test'). Плюс §15 'Kill / adjust criteria' требует ревью на каждом exit фазы и намеренно фронт-лоадит pain-closing value 'so sunk cost stays minimal if course-correction is needed' — механизм именно на случай непропорциональной стоимости уже встроен в план.

2) 'Duplicates T2' — механизм указан неточно. T2 (13.1) — fold-level property tests чистой lifecycle-машины (legal/illegal transitions, ordering, idempotent replay), без реального CLI/MCP surface, без hub/webhook-инъекций, без реальных credentials. Измерения grid'а, которые реально пересекаются с существующими тестами — surface (CLI vs MCP), уже покрытый T3 'CLI/MCP parity suite (7.1)', и injections (hub down/up, webhook lost/dup), уже покрытые T4 через F-1…F-9. Избыточность, если и есть, в основном относительно T3/T4, а не T2 — цитируемое обоснование аудитора не покрывает именно те измерения (surface/injections), которые дают основной комбинаторный вес.

3) Генерируемый matrix — прямая реализация явно одобренного top-level requirement R-016 ([01-vision.md:123](plan/01-vision.md#L123)): 'Full e2e test matrix: object types × lifecycle × roles × failure injections; every corner case mapped to a test | §12, §13'. Рекомендация аудитора удалить эту секцию и переписать R-016 — это откат уже утверждённого operator-level требования, а не починка дефекта реализации плана.

4) 'Single largest hidden build cost in the plan' — превосходная степень не подтверждена: L4 бандлит dashboard (graph+boards+contracts+health), full runbook set, succession note, handoff directive live И matrix — matrix лишь один из нескольких сопоставимо крупных пунктов той же фазы.

Здравое зерно есть (grid может быть избыточен относительно T3/T4, не T2, и стоит сузить объём), но заявленный дефект в изложенном виде (hidden, majorly duplicative именно с T2, требует удаления/ослабления R-016) текстом плана не подтверждается.
- [03-domain.md §3.4.2 vs §3.4.3 + 07-client.md OP-211] The requirement lifecycle is a second, near-identical state machine with a different verb vocabulary (publish/acknowledge/satisfy/withdraw vs submit/ack/respond/verify/cancel), and OP-211 exposes no `satisfy` or `withdraw` verb, so the CLI mapping is unspecified. || REJECTED: Grant the literal facts, refute the load-bearing claims.

Literally true: OP-211's token list (`ack/accept/decline/start/block/respond/verify/dispute/close/cancel/supersede`) contains neither "satisfy" nor "withdraw", and publish~submit / satisfy~verify / withdraw~cancel are indeed parallel roles across the two tables.

But "unspecified" and "cannot produce satisfy" are refuted by the plan's own mechanics:

1. §3.5 (03-domain.md:154-156): an event's required fields include a "transition name" and "resulting-state claim" — the CLI verb the user types and the transition value written to the event file are already two different things. OP-211's entry for `ack` itself proves this: the table's transition tables (3.4.2/3.4.3) name the event "acknowledge", not "ack" — so OP-211 is a catalog of CLI command names, not literal event-transition names, even for the exchange machine it does cover.
2. A2-examples.md B.11 (lines 277-291) is the concrete instantiation: axon (the requester) emits `transition: satisfy` / `state: satisfied` against `XR-axon-country-vocabulary` — and the surrounding prose literally labels this "verify (closes B.7's parent)". That is the plan's own worked example already doing exactly what the auditor recommends ("verify emits satisfy on requirements") — it is not an implementation-time invention, it's specified behavior, just specified by cross-reference (§3.5 + §3.4 table + example) rather than spelled out as a sentence in §7.

The "second, near-identical state machine" framing is also overstated: 3.4.2 (requirement) has 7 states/7 transitions with no accept/start/in_progress/block/respond/dispute stages at all, versus 3.4.3's 11 states/12 transitions — structurally different, not a copy with renamed verbs. And the vocabulary split (publish vs submit) is a deliberate, pervasive pattern: contract (3.4.1, standing) also uses "publish"; every exchange type (question/work_request/proposal/handoff/response) uses "submit". Unifying requirement onto the exchange FSM, as the auditor's primary recommendation suggests, would break this S-class/X-class convention used across all 7 lifecycle tables, not just fix requirement.

One genuine but minor gap, not the claimed defect: the withdraw~cancel mapping has no worked example the way satisfy does (CC-031 in 12-corner-cases.md only names the folded state `withdrawn`, not a CLI invocation), and no single sentence anywhere states "OP-211 verbs are type-polymorphic, the binary resolves the correct §3.4 transition name from artifact type + current state." An implementer derives this correctly by reading §3.5 + §3.4 + OP-211's own "author the corresponding event" phrasing + the B.11 example, but it costs cross-referencing. Worth a one-line clarifying sentence in §7.2, not a redesign — does not rise to "major" and does not support the "run requirements on the generic exchange machine" recommendation.
