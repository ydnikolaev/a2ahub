- [stale-refs] [§11.1 Hub dashboard, Contracts view (11-observability.md:17)] The Contracts dashboard view content inventory still reads "provider, version, stability, code-backed?, registered consumers, deprecations…" — `stability` is the removed field (D-023). ==> Replace the `stability` column with the folded lifecycle state (draft/published/deprecated/retired), which is what now carries that meaning.
- [stale-refs] [§5.2.1 Per-type extensions (05-schemas.md:55)] The correct 'no `stability` field' statement cites the wrong decision: "…never frontmatter (D-017)". D-017 is the fold-order / event-based-state decision; the decision that removed `stability` frontmatter is D-023. ==> Change the citation from (D-017) to (D-023) — or list both, since D-023 is the specific authority for dropping the field.
- [stale-refs] [Appendix B, event example (A2-examples.md:284)] The second lifecycle-event example is captioned "verify (closes B.7's parent)", but its body is a `satisfy` transition (transition: satisfy / state: satisfied) on requirement XR-axon-country-vocabulary. Under the D-024 closure model, `verify` targets a response (`XS`) and `close` targets the parent — that split applies to exchange types (question/work_request); requirements close via the requester's `satisfy` event (§3.4.2). ==> Relabel the caption to "satisfy (folds the requirement to satisfied, referencing response B.7)" so the header matches the event and doesn't reuse the verify/close vocabulary for a requirement.
- [stale-refs] [00-STRUCTURE.md §3 taxonomy annotation (00-STRUCTURE.md:82-84)] The structural-contract §3 still enumerates the pre-merge taxonomy — "exchange requests: question · work_request · decision_request · data_request · contract_change_proposal" — plus old roles ("machine (write-scoped)", §2). This predates the 8-type merge (D-004) where decision_request/data_request/contract_change_proposal became categories, not types. ==> Add a one-line pointer at §3/§2 of 00-STRUCTURE noting the taxonomy/roles shown are the original brief and that §3 (8 types, D-004) is authoritative — or annotate the merged candidates inline.
- [lifecycle-walk] [11-observability.md §11.1 (line 17) vs §17 D-023] The Contracts dashboard view lists a 'stability' column ('catalog: provider, version, stability, code-backed?, ...'). D-023/§5.2.1 removed the stability field; a contract now has only a folded lifecycle state (published/deprecated/retired). ==> Rename the column to reflect folded lifecycle state (e.g. 'status') or drop it, to avoid re-implying the abolished stability field.
- [lifecycle-walk] [A2-examples.md B.11 (line 284)] The event example is annotated 'verify (closes B.7's parent)' but its body is `transition: satisfy, state: satisfied` on requirement `XR-axon-country-vocabulary`. Requirements use the `satisfy` transition (§3.4.2), not the verify/close closure model (which is for exchanges, §3.4.6). ==> Fix the annotation to '— satisfy' and drop the 'closes ... parent' wording; requirements do not have a parent-close closure step.
- [lifecycle-walk] [03-domain.md §3.4.3 (line 130) vs 07-client.md OP-211 / 05-schemas.md §5.2.2] `unblock` is a lifecycle verb in OP-211 (line 38) and a value in the §5.2.2 transition enum, but §3.4.3 defines it only in prose ('unblock returns to prior state') with no From/Event/To table row. 'Prior state' also requires the fold to remember the pre-block state, which no table specifies. ==> Add an explicit `blocked --unblock--> <prior state>` row to §3.4.3 and state how the fold recovers the pre-block state.
- [lifecycle-walk] [03-domain.md §3.4.4 (line 148) vs 07-client.md OP-211 (line 38)] OP-211 claims 'lifecycle verbs = §3.4 transition names exactly, one verb per transition' but omits the decision `propose` transition (draft→proposed), which also has no other command mapping. §8.2 routes decisions through `a2a submit`, but OP-205 submit emits a `submit` event, not `propose`. ==> Either add a `propose` verb/step or state explicitly that `a2a submit` emits the type-appropriate first-transition event (propose for decisions, publish for requirement/announcement), so the propose event has a defined authoring path.
- [lifecycle-walk] [03-domain.md §3.4.7 (line 180) vs 05-schemas.md §5.2.2 (line 76)] Announcement `expired(valid_until)` is a state reachable with no named actor/event, yet `expired` falls inside the §5.2.2 transition enum ('union of all §3.4 transition names'). It is unclear whether `expired` is an emitted event (who authors it?) or a fold-computed overlay from `valid_until`. ==> State whether expiry is a fold-computed state (then exclude `expired` from the event transition enum) or an event (then name its actor).
- [lifecycle-walk] [03-domain.md §3.5 (line 201) vs 05-schemas.md §5.2.2] §3.5 lists 'resulting-state claim' as a required event field and §5.2.2 does not mark `state` optional, but the two transition-free event kinds (`note`, broadcast `acknowledge`) 'carry no state transition' and thus have no resulting state. ==> Mark `state` optional (or explicitly N/A) for note and broadcast-ack events in §5.2.2.
- [lifecycle-walk] [00-STRUCTURE.md (lines 77-86)] The taxonomy outline still lists the pre-D-004 candidate types: `decision_request`, `data_request`, `contract_change_proposal`, and `incident/alert (confirm need)` — none of which are among the final 8 types (§3.1), where they became work_request/announcement categories. ==> Update the outline to the 8-type taxonomy, or add a note that D-004/§3.1 supersede it (the '(to be confirmed in interview)' hedge is now resolved by Q-009).
- [id-integrity] [11-observability.md §11.1 (Contracts dashboard view)] The dashboard Contracts view column list still reads "provider, version, stability, code-backed?, registered consumers, deprecations" — includes a `stability` column. ==> Rename the column to the folded lifecycle state (e.g. "status" = published/deprecated/retired) or drop it.
- [id-integrity] [05-schemas.md §5.2.1 (contract row citation)] The no-stability rationale in §5.2.1 cites "(D-017)": "draft/published is folded lifecycle state, never frontmatter (D-017)". D-017 is the fold-order / events-as-transitions decision; the decision that actually removes stability is D-023 ("no `stability` frontmatter (folded state only)"). ==> Change the citation to D-023 (or D-017+D-023).
- [id-integrity] [12-corner-cases.md CC-008] CC-008 ends with "(see CC-034)", but CC-034 does not exist — section B (Lifecycle) runs CC-020…CC-032 with no CC-034. ==> Repoint to CC-062 (or CC-032).
- [id-integrity] [08-agent-protocol.md §8.7 (expert skill contents)] §8.7 cites "a worked decompose example — B.13", but Appendix B's B.13 is the consumer registry (`axon/consumes.yaml`). No worked decompose example exists in Appendix B at all. ==> Either add the decompose example as a new B.# entry and point §8.7 at it, or correct the reference to the intended example.
- [id-integrity] [A2-examples.md B.11 (second lifecycle event)] The second event's header comment reads "verify (closes B.7's parent)", but the event is `transition: satisfy` / `state: satisfied` on `subject: XR-axon-country-vocabulary` — a requirement satisfy, not a verify/close. ==> Relabel the comment to "satisfy (requester's satisfy event, folds the requirement to satisfied; refs the response + contract version)".
- [id-integrity] [A2-examples.md B.12 / B.13 ordering] Appendix B presents B.13 (consumer registry) before B.12 (space manifest) — the numbering is out of sequence. ==> Swap so B.12 precedes B.13, or renumber.
- [fresh-eyes] [§3.4.6 / §5.2.1 response `parent`, plus §1.3 step 6, §8.2 step 6, E2E-1 (13-testing.md:36), and B.11 label (A2-examples.md:284)] Requirement completion is consistently described with exchange-closure vocabulary that its own state machine (§3.4.2) does not use. §3.4.6 and §5.2.1 call the response's `parent` an "exchange ID", but golden fixture B.7 sets `parent: XR-axon-country-vocabulary` (a requirement, class "S — standing", not exchange). §1.3 step 6, §8.2 step 6, and E2E-1 say the requester "verifies... and closes" / "response → verify → close", but §3.4.2 has no verify/close transition on requirements — it folds via `satisfy`. B.11's satisfy event is even captioned "verify (closes B.7's parent)". ==> State that a response's `parent` may be any answerable artifact (exchange OR requirement); use "parent artifact" not "parent exchange". Align §1.3/§8.2/E2E-1 wording to `satisfy` for requirements and relabel B.11's second event from "verify" to "satisfy".
- [fresh-eyes] [§5.2.1 (05-schemas.md:55)] The parenthetical justifying no-stability cites D-017 ("never frontmatter (D-017)"). D-017 is the fold-order decision; the stability/publish-event decision is D-023. ==> Change the citation from D-017 to D-023.
- [fresh-eyes] [§9.2 Onboarding runbooks (09-human-ops.md:24-26)] The "New system into an existing space" list has two consecutive steps numbered "3." (project-dev install, then publish hello-world announcement) before step 4. ==> Renumber the second "3." to 4 and the hub step to 5.
