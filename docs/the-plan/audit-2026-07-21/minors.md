- [better] [05-schemas.md §5.7 (and §5.1 YAML frontmatter)] Digest canonicalization ('frontmatter normalized + body bytes') is unspecified, and the YAML parsing profile (1.1 vs 1.2 typing — the Norway problem, auto-typed dates) is unpinned, yet pinned `id#digest` refs persist in immutable history across binary versions and, later, across independent implementations. ==> Freeze the canonical form in the plan: e.g. JCS (RFC 8785) over the parsed frontmatter object + raw body bytes, YAML parsed under a stated 1.2 core-schema profile, with test vectors in T1 that lock it forever.
- [better] [08-agent-protocol.md §8.5 (vs 03-domain.md §3.4/§3.5)] The escalation ladder mandates an in-thread reminder via 'an event note', but the domain model defines events strictly as state transitions — no note/comment event kind exists, so the prescribed reminder action is undefined. ==> Add a no-op `note` event kind to §3.5 (valid on any open exchange, folds to no state change, renders in thread view), and reference it from §8.5.
- [better] [05-schemas.md §5.1/§5.4 (envelope versioning)] Envelope versions are single-integer (`envelope/v<N>`) while §5.4 says envelope evolution 'follows the same rules' as contract semver — additive (minor) envelope changes have no representable version, so it is ambiguous whether adding an optional field bumps N (forcing the N/N−1 fleet dance) or silently changes v1. ==> Specify envelope versioning explicitly: either `envelope/v<major>.<minor>` with 'validators accept same-major unknown-minor by ignoring unknown optional fields', or state that N bumps only on breaking change and additive fields are always tolerated (adjust the unknown-key rule accordingly).
- [better] [07-client.md §7.2 OP-205 (with CC-046)] `a2a submit` wraps commit+push to a shared main, but its behavior on non-fast-forward rejection (another system pushed first) is unspecified — CC-046 covers merge of non-conflicting files, not push contention and the required fetch/rebase/retry loop. ==> Specify OP-205's contention behavior: fetch → rebase own commit → re-validate (V2) → push, bounded retries, and the failure mode when rebase conflicts (impossible by construction for new files; define for event appends).
- [better] [13-testing.md §13.1 T5 (vs AC-101.2, CC-060/063)] T5/T6 must prove host-side enforcement (CODEOWNERS merge blocks, branch protection, PAT scope rejection), which cannot be exercised against local throwaway git fixtures — the plan never says how CI obtains real GitHub repos, teams, and three scoped credentials. ==> State the approach in §13: an ephemeral test org with scripted repo/team provisioning for the host-enforcement subset (run pre-release, not per-commit), with everything else on local git fixtures; mark which CCs require the real-host tier.
- [better] [12-corner-cases.md CC-082 × CC-062] The interaction of the strict retire-block with offboarding is unspecified: a registered consumer that goes `left` without acking a deprecation blocks the provider's retire forever. ==> Specify that participants with manifest status `left` are excluded from the registered-consumer ack set at fold/validation time (with a flag on the dashboard), and add the combined case as a new CC.
- [better] [10-security.md §10.4 G5 (with CC-010)] The G5 'human override' of a validator block is named but has no specified mechanism — nothing says how a human override is expressed, recorded, or verified at V2/V3. ==> Specify the override channel: e.g. override only via the PR path with an approving review by a system owner plus a machine-readable marker the validator checks and the audit log records; forbid override on the direct-push path.
- [better] [07-client.md §7.3 (with 10-security.md T-8)] Fleet-wide self-update (`a2a update`) verifies only checksums fetched from the same release channel — no release signing, so a compromised product repo or release pipeline compromises every participant's write credential at once. ==> Sign releases from day one (minisign/sigstore — one keypair, near-zero ops) with the public key embedded in the binary and verified by `a2a update`; add supply-chain compromise of the release channel to the §10.2 threat table.
- [simpler] [07-client.md §7.7 vs §7.2] The MCP tool list includes `a2a_search` and `a2a_contract_get` with no CLI counterparts in OP-201..219, contradicting the CI-enforced CLI/MCP parity invariant declared in §7.1. ==> Either add the corresponding CLI operations to §7.2 or trim the MCP list to the true OP-2xx mirror; don't let the plan itself violate the invariant it mandates a test suite for.
- [simpler] [05-schemas.md §5.2.1 (`compat_policy`) + A2-examples.md B.1] `compat_policy` is a required contract field whose value set is never defined anywhere ('5.4 ref'; the fixture just says `default`). ==> Drop the field; §5.4 semver policy is global. Reintroduce when a second policy actually exists.
- [simpler] [04-topology.md §4.4 + 02-actors.md §2.3] Vendored-mirror machinery (manifest maintainer designation, envelope metadata, dashboard staleness surfacing) is specified in full though v1 has zero vendored dependencies (`sot` is a planned participant, not a vendor). ==> Reduce §4.4 to the folder convention plus a one-line envelope note, and mark the manifest field and dashboard staleness surfacing as activated-on-first-use, so no v1 code path exists for it.
- [simpler] [04-topology.md §4.3 + 12-corner-cases.md CC-070/CC-073 + 13-testing.md E2E-10] Cross-space reference grammar, unresolvable-ref warning logic, fleet-wide space-ID registry, and a dedicated federation e2e are all specified for a v1 that has exactly one space. ==> Keep the `space:id` syntax reserved in the grammar, but defer resolution/warning behavior and E2E-10 to the phase where a second space is created.
- [simpler] [03-domain.md §3.2 vs 12-corner-cases.md CC-009] §3.2 asserts 'the validator rejects a question living inside an announcement', but CC-009 correctly concedes schema validation cannot see prose and the real remedy is a decline with reason `split-required`. ==> Reword §3.2 to match CC-009: single-intent is enforced by template guidance + recipient decline, validated structurally only where the envelope makes it visible.
- [simpler] [06-hub-service.md §6.2 OP-106 vs 07-client.md §7.6 / 11-observability.md §11.2] OP-106 says the hub graph endpoint 'feeds dashboard and local HTML', but §7.6/§11.2 define local HTML as generated from the local cache, offline, hub-optional. ==> Delete 'and local HTML' from OP-106; the local view renders from the binary's own cache by design.
- [simpler] [03-domain.md §3.5] Lifecycle events carry a 'resulting-state claim' field that is redundant with the deterministic fold and can only ever agree or diverge from it. ==> Drop the state claim from the event schema (transition name suffices); if wanted for debugging, mark it explicitly non-normative and excluded from validation.
- [simpler] [A2-examples.md B.7/B.8 vs 05-schemas.md §5.2 ref grammar] Normative golden fixtures use ref forms the grammar doesn't allow: `id@version#digest` (B.7) and `id#body` (B.8), while §5.2 permits only `id@version` | `id#digest` | `space:id…`. ==> Fix the fixtures to the defined grammar or extend §5.2's grammar explicitly — decide now, not in T1 triage.
- [simpler] [08-agent-protocol.md §8.5 (stale-item reminder)] The escalation ladder prescribes reminding via 'an event `note`', but §3.5 defines events strictly as state transitions and OP-211 has no note/remind verb — a no-op annotation event doesn't exist in the model. ==> Either add a `note` non-transition event kind to §3.5/OP-211 (folded as no-op, displayed on the thread) or change §8.5 to a defined mechanism; today the instruction is unexecutable.
- [simpler] [06-hub-service.md §6.2 SSE + 11-observability.md §11.1] A Server-Sent-Events realtime stream with 'live pulse' is specified for a dashboard whose event volume is a handful of commits per day across 2–10 systems. ==> Ship the dashboard on short-interval polling of OP-106/108 (identical UX at this volume, less hub machinery); note SSE as an optional later upgrade rather than v1 scope.
- [simpler] [11-observability.md §11.3 + 06-hub-service.md §6.2/§6.5 + 04-topology.md §4.2] Notification routing config is placed in three places across the plan: `space.yaml` ('notification routes', 4.2 layout comment), `hub.yaml` ('subscriptions are hub config… not per-user', 6.2), and 'manifest/hub config' (11.3). ==> Pick one home — hub.yaml, since only the hub delivers notifications — and remove the routes field from the space manifest layout comment and the 11.3 ambiguity.
- [simpler] [02-actors.md §2.3 + 09-human-ops.md §9.3] The space manifest is loaded with operational bookkeeping that will rot: informational runtime-channels per participant, and credential records (who/scope/expiry) maintained by PR. ==> Trim `space.yaml` to what tooling actually reads (system, org, section, owners, status, joined); keep credential inventory in the operator's `hub.yaml`/runbook notes.
- [implementer-gaps] [08-agent-protocol.md §8.1 + 07-client.md §7.2] The protocol's entry points `a2a inbox --actionable` and `a2a outbox --attention` use filters whose semantics are never defined (which states/conditions qualify as 'actionable'/'attention'). ==> Define both filters as explicit state/predicate lists in §7.2 (e.g. actionable = inbound in submitted|acknowledged awaiting me + responded awaiting my verify + p1/blocking anything).
- [implementer-gaps] [06-hub-service.md OP-107 + 11-observability.md §11.3] Staleness is half-defined: `needed_by` overdue is clear, but 'silent past SLA defaults' (OP-107) and 'staleness threshold crossed' (11.3) name defaults and thresholds that are never given values or a config surface. ==> Define default SLAs per state (e.g. submitted unacked > 2 business days = silent-stale), where they're configured (space.yaml vs hub.yaml), and one shared staleness predicate in the core library.
- [implementer-gaps] [04-topology.md §4.2 (space.yaml comment) vs 06-hub-service.md §6.2/§6.5 vs 11-observability.md §11.3] Where notification routing lives is contradictory: §4.2's layout comment puts 'notification routes' in space.yaml, §6.2/§6.5 say routing/subscriptions are hub config (`hub.yaml`, 'not per-user state'), and §11.3 says 'opt-in per space via manifest/hub config'. ==> Pick one owner (hub.yaml for v1, matching §6.2), delete the space.yaml mention, and note manifest-level routing as a future extension.
- [implementer-gaps] [03-domain.md §3.7 + A2-examples.md B.12 (`gates: default`)] Gate configurability ('stricter allowed, looser not below G1–G3') has no defined config syntax beyond the literal `gates: default` in the example. ==> Define the `gates` object schema: default expansion, allowed overrides, and the validation rule rejecting below-floor configurations.
- [implementer-gaps] [05-schemas.md §5.2.1 + A2-examples.md B.1 (`compat_policy: default`)] `compat_policy` is a required contract field whose value space is undefined — §5.4 describes one policy but names no policy identifiers; the example uses `default`. ==> Either define the enum (e.g. `default` only in v1, reserved for future) or drop the field until a second policy exists.
- [implementer-gaps] [12-corner-cases.md CC-006/CC-012/CC-027 + 03-domain.md §3.3/§3.8] Several load-bearing constants are unvalued: artifact size limit ('configurable, generous'), clock-skew tolerance window, dispute-warn threshold placement, the base32 alphabet/case for `<rand4>`, and the timezone of `<YYYYMMDD>` in IDs and of event-year sharding. ==> Add a 'defaults and constants' table to §5 or §0: size limit default, skew tolerance, Crockford-base32-lowercase (or chosen alphabet), UTC everywhere.
- [implementer-gaps] [05-schemas.md §5.2 vs A2-examples.md B.9/B.2] Date field formats are inconsistent between spec and golden fixtures: `valid_until` is 'RFC 3339' in §5.2 but `2026-10-01` (date-only) in B.9; `joined: 2026-07-28` in B.12 has no declared format; `needed_by` is 'RFC 3339 date' (ambiguous: full-date or date-time?). ==> Declare per-field whether full-date or date-time is expected and fix either §5.2 or the examples to match.
- [implementer-gaps] [05-schemas.md §5.5 + 13-testing.md T1] Machine-readable validation error codes are 'catalogued for §13 fixtures' but no code scheme, namespace, or seed catalog exists anywhere in the plan. ==> Define the code scheme (e.g. `E-SCHEMA-###`, `E-REF-###`, `E-LIFE-###`, `E-POLICY-###` mapped to the four validation classes) and require the catalog file's location in the product repo.
- [implementer-gaps] [14-us-ac.md US-701, US-802, US-902] Three user stories have prose bracket notes instead of numbered Given/When/Then ACs, violating the doc set's own structure contract (§14 per 00-STRUCTURE.md) and leaving their exit conditions unenumerated. ==> Write AC-701.x, AC-802.x, AC-902.x in the standard form.
- [implementer-gaps] [03-domain.md §3.4.3] The transition guards use undefined state sets: 'any open', 'any pre-satisfied', 'submitted…in_progress' — the exact membership of 'open' (does it include draft? blocked? responded?) is never enumerated, and `unblock` 'returns to prior state' requires the fold to track a prior-state stack that is unspecified. ==> Add a state-classification table (open/terminal per type) and specify unblock as carrying the resume-state explicitly in the event (or define fold-side prior-state memory).
- [implementer-gaps] [03-domain.md §3.1/§3.4.3 (proposal)] Proposal semantics are ambiguous: §3.1 says proposals are answered 'accept/decline', but proposals share the question/work_request state machine where `accept` means 'will do' and closure still requires respond+verify — whether an accepted proposal needs an XS response and sender verification to close is undefined. ==> State the proposal happy path explicitly (e.g. accept ⇒ respond with XS referencing the resulting change ⇒ sender verify), or give proposal its own reduced table.
- [implementer-gaps] [07-client.md §7.2 (idempotency note) + 14-us-ac.md AC-301.1] 'Every mutating command is idempotent by artifact/event ID' is unimplementable as stated for lifecycle verbs: each invocation mints a fresh event ULID, so nothing keys the dedup — the actual mechanism (skip when folded state already reflects the transition?) is unspecified. ==> Define idempotency per verb as a fold-state predicate ('command X is a no-op iff folded state already ∈ {…}'), and note the exceptions (repeatable events like note/reminder).
- [implementer-gaps] [13-testing.md T5/T6 + 14-us-ac.md AC-101.2] The e2e strategy never states how GitHub-dependent mechanics (branch protection blocking, CODEOWNERS merges, webhooks, force-push alerts) are exercised in CI — T5 says 'simulated 3-system space in CI' with three credentials, which local git fixtures cannot cover. ==> State the harness decision: which CCs/ACs run against local bare repos + simulated webhooks, which run against a dedicated test GitHub org, and which are runbook-verified only.
- [implementer-gaps] [05-schemas.md §5.1 + 16-handoff-directive.md + 13-testing.md] The product repo itself — name, layout (where schemas, templates, space template, cc-coverage.yaml, harness texts live), and the 'stable URIs' schemas are published under — is never specified, though multiple sections reference its contents. ==> Add a short normative product-repo layout section (or appendix): repo name, top-level directories, schema URI scheme (e.g. https://…/schemas/envelope/v1), release asset naming.
- [implementer-gaps] [05-schemas.md §5.2 (`to` on contracts) + A2-examples.md B.1] The semantics of `to` on standing artifacts is undefined: B.1's contract lists `to: [seomatrix]`, but contracts serve any future consumer — whether `to` must be updated as consumers register, and what it means for inbox computation, is unstated. ==> Define `to` on standing types as initial-notification targets only (inbox shows them once), with consumer discovery going through the registered-consumer registry, and state that published-version immutability excludes addressing metadata or that `to` is fixed at publish.
- [user-agent-gaps] [03-domain.md §3.1 vs 05-schemas.md §5.2.1 (response)] A proposal's accept/decline outcome is not expressible: §3.1 says proposal 'responds: yes (accept/decline)' but the XS `result` enum is answered|delivered|partial|cannot, with no accepted/rejected value. ==> Either extend the XS result enum for proposal parents (accepted|declined) or specify that proposals are resolved via the decline/respond lifecycle events alone and drop 'accept/decline' from 3.1.
- [user-agent-gaps] [05-schemas.md §5.2 + 08-agent-protocol.md §8.5] A `blocking: true` item without `needed_by` never triggers any staleness or escalation path: the 8.5 ladder's sender-side row fires only 'past needed_by', and nothing requires needed_by when blocking is true. ==> Validator rule: `blocking: true` requires `needed_by` (or a documented default staleness SLA applies to blocking items); add a ladder row for silent blocking items.
- [user-agent-gaps] [08-agent-protocol.md §8.2 step 6] Verification duty is defined only against acceptance_criteria, which are optional on `question` — the close path for the most common lightweight exchange type is under-guided. ==> One sentence in 8.2: for questions without ACs, verify = confirm the answer resolves the stated `expected_response.shape` and is actionable in your context; close promptly — an open answered question is protocol debt.
- [security] [10-security.md §10.6 (public-mode readiness checklist)] The public-mode checklist is incomplete for the stated 'nothing in v1 may contradict it' bar. Missing: a read-authorization model for public/untrusted spaces (the list only replaces the WRITE path); output sanitization/XSS hardening for a dashboard that would render untrusted public content; a participant revocation/blocklist mechanism; anti-abuse for storage/artifact-volume flooding (only 'rate limiting' is named, undefined); and it does not flag that v1 section-write isolation is already unenforceable (findings above) and must be fixed before public mode. ==> Extend §10.6 with: public read-authz per space, mandatory HTML sanitization/CSP, participant revocation, storage/volume abuse limits, and an explicit note that per-section write enforcement must move to hub-mediated write before untrusted parties join. Cross-reference the section-isolation and XSS findings.
- [security] [10-security.md §10.4 (restricted ⇒ bilateral space) + §17 Q-001] 'restricted' classification is enforced only as a membership rule (bilateral-space) and there is no encryption in v1 (Q-001 deferred). The plan does not state the residual: restricted content is still fully readable by GitHub and by the operator via the hub's all-space read PAT, so 'restricted' offers no confidentiality against the host or the operator — only against other org members. ==> State this residual explicitly in §10.4: without per-artifact encryption, no classification level protects content from the git host or the hub operator. Note it as a v1 acceptance and tie it to when Q-001 must close.
- [security] [03-domain.md §3.5 (fold sorts by ULID) + 12-corner-cases.md CC-022] Fold ordering and conflict resolution depend on ULID order, but a ULID's timestamp component is chosen by the event author. CC-022 says 'ULID order wins' for near-simultaneous accept/decline — an actor who controls one event can mint a ULID with a chosen (earlier or later) timestamp to deterministically win or lose the race. ==> Note that ULID timestamps are author-controlled and therefore not a trustworthy tiebreaker across parties; where cross-party conflict resolution matters, resolve by git commit order/receipt at the hub or flag both and require human resolution rather than silently letting author-chosen ULIDs decide.
- [consistency] [12-corner-cases.md CC-022 vs 03-domain.md §3.4.3] CC-022 claims that for near-simultaneous accept + decline 'the loser becomes an illegal-transition flag', but decline is legal from `accepted` (decline's From range 'submitted…in_progress' includes it), so accept-then-decline folds cleanly to declined with no flag; only decline-then-accept produces a flag. ==> Reword CC-022: ULID order wins; a flag is raised only if the later event is illegal from the folded state (e.g. accept after decline), and pick an example pair that always conflicts (e.g. respond + cancel).
- [consistency] [03-domain.md §3.4.7 `expired(valid_until)` vs §3.5 fold rule 4 and §3.4.3 no-auto-transition stance] `expired` is a state entered by wall-clock passage of `valid_until` with no authoring event, contradicting both the no-auto-transition principle (§3.4.3, S-7 rationale) and fold determinism ('same event set ⇒ same state') since the folded state would depend on evaluation time. ==> Make expiry a derived staleness flag (like `needed_by`) rather than a lifecycle state, or require an explicit expire event.
- [consistency] [03-domain.md §3.4.3 range notation ('submitted…in_progress') vs `blocked` state] The From-ranges are ambiguous with respect to the side-state `blocked`: decline ('submitted…in_progress'), cancel ('draft…in_progress') and respond ('accepted/in_progress/acknowledged') all exclude `blocked` on a literal reading, so a blocked exchange can be neither declined, cancelled, nor responded to without an unblock event that has no table row of its own. ==> Enumerate From-states explicitly per row (no ellipsis ranges), add an `unblock` row with its prior-state memory rule, and state which events are legal from `blocked`.
- [consistency] [12-corner-cases.md CC-008 → 'CC-034'] CC-008 references CC-034, which does not exist (section B ends at CC-032); the intended target is presumably CC-062 (orphaned-counterparty). ==> Point CC-008 at CC-062.
- [consistency] [09-human-ops.md §9.4 'Q-scale'] §9.4 cites an open question as 'Q-scale', which is not a valid §17 ID; the defined item is Q-002. ==> Replace 'Q-scale' with Q-002.
- [consistency] [01-vision.md S-6 vs 09-human-ops.md §9.1 vs 14-us-ac.md US-901] Onboarding time budgets conflict: S-6 says a new system onboards in ≤1 hour, §9.1's project-dev profile says ≤30 min, and US-901 claims full onboarding 'by runbook alone in ≤30 min' while its AC cites S-6. ==> State one number per scope: install profile ≤30 min, full onboarding (incl. G4 PR) ≤1 h, and align US-901's text with the scope it actually tests.
- [consistency] [00-meta.md §0.2 ID conventions] Prefixes used normatively throughout are missing from the ID table: G# (gates, §3.7), V# (validation points, §5.5), F-# (hub failure modes, §6.7), T# (test tiers, §13.1), E2E-# (§13.3), T-# (threats, §10.2) — and T-# (threats) is one hyphen away from T# (tiers), an active collision (CC-027's 'verify-fail ×2' also mixes handoff vocabulary into a dispute case). ==> Add all live prefixes to §0.2 and rename either threats (TH-#) or tiers to remove the T collision.
- [consistency] [00-meta.md §0.4 glossary vs 00-STRUCTURE.md §0 done-when] The §0 exit criterion ('every term used in §1–§17 is defined here') is unmet: `fold`, `thread`, `gate`, `registered consumer`, `manifest`, `classification` are load-bearing terms with no glossary entry; org canonical IDs are also inconsistent (0.5 lists org 'Misha' while B.12 records `org: seomatrix`). ==> Add the missing glossary entries with section pointers and fix §0.5 to list canonical org IDs, not human names.
- [consistency] [06-hub-service.md §6.5 vs 11-observability.md §11.3] Default chat routing conflicts: §6.5 routes 'validation flags' to the chat webhook by default, while the §11.3 matrix marks 'validation flags on your section' as statusline-only (chat ✘); §6.5 also asserts 'Telegram first' as decided while Q-007 still holds it open. ==> Make §11.3 the single normative matrix, have §6.5 reference it without restating defaults, and align the Telegram wording with Q-007's status.
- [consistency] [14-us-ac.md AC-201.1 vs CC-009] AC-201.1's fixture range 'CC-001…011' includes CC-009, whose expected behavior is explicitly NOT a validator failure (prose-level smuggling is handled socially), so no invalid fixture with an expected error code can exist for it. ==> Cite 'CC-001…008, 010, 011' in AC-201.1 and map CC-009 to a behavioral/e2e test (decline with reason `split-required`).
- [consistency] [05-schemas.md §5.2 `category` vs §5.2.1 '—' types and A2 examples B.6/B.7/B.8/B.10] `category` is listed as required in the base envelope (no `?`), but §5.2.1 gives decision/response/handoff/status_report no enum ('—') and all four golden examples omit the field. ==> Mark `category` as per-type: required where an enum exists, absent (forbidden) for the '—' types.
- [consistency] [05-schemas.md §5.2 ref grammar / §3.5 actor block vs A2-examples.md B.7, B.9, B.11] Fixtures use forms the schemas don't define: B.7's ref `XC-…@2.2.0#sha256:9e1a…` combines @version and #digest, absent from the §5.2/§3.8 grammar; B.11 event actor blocks add a `system` key not in the §2.1 actor block; B.9's `valid_until: 2026-10-01` is date-only where §5.2 says RFC 3339 (timestamp). ==> Extend the ref grammar to allow `id@version#digest`, add `system` to the event actor schema (it is needed for authorization checks), and pin timestamp-vs-date types per field.
- [consistency] [02-actors.md §2.3 `suspended` status] The manifest status enum includes `suspended`, but no section defines its semantics — CC catalog, inbox computation, V2 `to:` checks (CC-008) and authz cover only `active` and `left`. ==> Either define suspended behavior (write-blocked, addressable-warn, items flagged) in §2.3 + a CC, or drop the value from v1.
- [consistency] [04-topology.md §4.4 vendored mirrors vs §3.1/§5 schemas] Vendored mirror files 'carry envelope metadata (source URL, retrieval date, digest)', but no object type, envelope extension, or schema for vendored entries exists in the 10-type taxonomy or §5 — and V3 validates the full repo including `vendored/`. ==> Define a minimal `vendored/v1` manifest schema (per-mirror metadata file) in §5.1 and state how V3 treats vendored payload content (skip or digest-only).
- [consistency] [07-client.md §7.7 vs §7.2 and R-018] §7.7 claims MCP tools 'mirror OP-203…OP-213 one-to-one' and that there are 'no MCP-only capabilities', yet lists `a2a_search`, which has no OP-2xx counterpart anywhere in §7.2 (and `a2a_contract_get`'s CLI mapping is unclear). ==> Add an `a2a search` command (new OP) to §7.2 or drop `a2a_search` from §7.7; state the OP mapping for `a2a_contract_get` (e.g. OP-209 with contract ref).
- [ops-realism] [04-topology.md §4.2 (decisions/ layout)] 'CODEOWNERS = all required parties' for decisions/ does not enforce multi-party approval — GitHub satisfies code-owner review with ONE approval from ANY listed owner of the path. ==> Reword §4.2: CODEOWNERS on decisions/ prevents non-participant merges only; quorum is enforced solely by the §3.4.4 fold over per-party approve events. 
- [ops-realism] [07-client.md OP-205 + 12-corner-cases.md CC-046] Concurrent direct pushes to the shared main from different machines are rejected as non-fast-forward — 'git merge naturally' (CC-046) does not happen on push, and OP-205 specifies no fetch-rebase-retry behavior. ==> Specify in OP-205: submit performs fetch → rebase (trivial, disjoint files) → push with bounded retries; on true content conflict, abort with CC-047 guidance. Correct CC-046's expected behavior accordingly.
- [ops-realism] [06-hub-service.md F-7 + 12-corner-cases.md CC-045] Force-push on a space is only alarmed after the fact, though GitHub can simply forbid it (block force pushes / non-fast-forward on the protected branch or ruleset). ==> Add to §4.2/§9.1: branch protection/ruleset MUST disable force pushes and branch deletion on main; keep F-7 as defense-in-depth for admin-bypass cases only.
- [ops-realism] [15-rollout.md L0 + 10-security.md §10.5 (CI credential row)] Space CI is specified to run 'a pinned prebuilt validator binary' using only the repo's default GITHUB_TOKEN, but that token cannot download release assets from the (private, pre-open-source) product repo. ==> Decide and state the distribution path: make product-repo releases public from day one, or provision an org-level read token / GitHub App for space CI and add it to §10.5 and the §9.1 space-admin runbook.
- [ops-realism] [13-testing.md §13.1 (T5) + §13.2] The host-enforcement layer (CODEOWNERS, branch protection/rulesets, PAT scope rejection — CC-060, CC-063, AC-101.2) cannot be exercised against local throwaway git fixtures, yet T5 claims full multi-party e2e in CI with 'three credentials'. ==> Split the matrix explicitly: T3/T5 run against local git for protocol semantics; add a small T6-adjacent 'host-enforcement conformance' suite that runs against a dedicated GitHub test org (or document those CCs as verified-by-runbook, not by test).
- [ops-realism] [05-schemas.md §5.2/§5.2.1 + A2-examples.md B.6/B.7/B.9] The envelope base table marks `category` as required (no '?') while four types (decision, response, handoff, status_report) define no category enum, and the golden examples omit it; `needed_by`/`valid_until` examples use date-only values against a field documented as RFC 3339. ==> Mark `category` as per-type (required only where an enum exists), and either declare `needed_by`/`valid_until` as full-date (ISO 8601 date) or fix the examples to full RFC 3339 timestamps.
