- [critical/implementer-gaps] [04-topology.md §4.2 + 05-schemas.md §5.5 (V3) + 10-security.md §10.3 + 12-corner-cases.md CC-060] The specified write-enforcement mechanics are not implementable on GitHub as described: CODEOWNERS only gates PR merges, so with direct pushes to `main` allowed (§4.2 'sections whose gates don't apply push directly'), nothing blocks a cross-section direct push; and V3's stated 'on failure: merge/push blocked (branch protection)' is impossible for direct pushes — required status checks apply only to PRs, so an invalid direct push lands on main. ==> Decide and specify the actual GitHub mechanism: either (a) all writes go through auto-merged PRs with required checks and CODEOWNERS, with the binary opening/merging PRs transparently, or (b) direct push allowed with V4-flag + revert runbook explicitly accepted as the enforcement level for non-gated writes — and rewrite §10.3 braces, CC-060 and AC-101.2 to match the chosen mechanism.
- [critical/implementer-gaps] [05-schemas.md §5.4 + 07-client.md §7.4 + 12-corner-cases.md CC-081] The 'registered consumer' set is partly defined by each project's local `.a2a/config.yaml` (declared dependencies), which lives outside the space — yet the space-side validator (V3 blocking retire, CC-081) and the hub (contracts view, deprecation targeting) are required to enforce and display it. ==> Move the consumer registry into the space (e.g. a per-system `consumes.yaml` in its section, written by `a2a`, mirroring the project-config dependencies), and define V3/hub as reading only that in-space registry; project config becomes a local convenience that syncs to it.
- [major/implementer-gaps] [03-domain.md §3.5 + 05-schemas.md + A2-examples.md B.11] §3.5 promises 'Full schema in §5' for lifecycle events, but §5 contains no event field table; the only sources are §3.5 prose and example B.11, which disagree with the envelope spec (event actor carries a `system` key absent from §5.2's actor shape; events carry a `state` claim whose handling on mismatch with the fold result is unspecified; events have no `space` field). ==> Add a normative event field table to §5 (mirroring §5.2's style): all fields, types, required/optional, the actor shape including `system`, and an explicit rule for when the `state` claim disagrees with the computed fold (e.g. 'claim is informational; mismatch ⇒ flag').
- [major/implementer-gaps] [03-domain.md §3.4 + 05-schemas.md §5.2] 'The artifact records its initial state' has no corresponding envelope field — §5.2 defines no `state`/`status` field — and it is unspecified whether draft artifacts are committed at all, and which event (if any) represents `create`/`submit` for each class. ==> Specify: (a) drafts are local-only / committed (pick one), (b) the initial folded state of an artifact present with zero events, (c) whether `create`/`submit`/`publish` produce event files or are implied by the artifact's first commit, per lifecycle class.
- [major/implementer-gaps] [07-client.md §7.2 OP-211 vs 03-domain.md §3.4] The lifecycle verb list (ack/accept/decline/start/block/respond/verify/dispute/close/cancel/supersede) is missing transitions the state machines require: `satisfy` (3.4.2), `withdraw` (3.4.2), `unblock` (3.4.3), `propose`/`approve`/`reject` for decisions (3.4.4), `publish` for requirements/broadcasts, and handoff's `verify-pass`/`verify-fail` use different names than any listed verb. ==> Extend OP-211 (or add OPs) to cover every transition name in §3.4 exactly, with an explicit verb→transition→event mapping table per object type.
- [major/implementer-gaps] [03-domain.md §3.4.3/§3.4.6 + 12-corner-cases.md CC-029 + 07-client.md OP-211] Closure semantics are contradictory: §3.4.3 has only `verify → closed`, §3.4.6 says the parent closes on the sender's `verify` event, but CC-029 says the parent 'stays responded until sender's close event', and OP-211 lists `verify` and `close` as distinct verbs. ==> Define normatively: per-response verification events (subject = XS id) vs parent closure event (subject = parent id), when close is implicit (single response verified) vs explicit, and reconcile §3.4.3, §3.4.6, CC-029 and OP-211 to one model.
- [major/implementer-gaps] [03-domain.md §3.3 + 05-schemas.md §5.2 + A2-examples.md B.6] Decisions break two stated invariants with no stated exception: B.6's ID is `XD-getvisa-…` (space-named) while §3.3 mandates `<system>` = authoring system, and decisions live in `decisions/` while §5.2 requires `from` to 'match owning section' and `id` to be 'validated against filename and section'. ==> Specify the XD ID scheme explicitly (and fix either §3.3 or B.6), and enumerate the section-validation rules per path class: `<system>/…` artifacts, `<system>/events/…`, `decisions/`, `vendored/`.
- [major/implementer-gaps] [05-schemas.md §5.2 (`to` list) + 03-domain.md §3.4.3] Exchanges may address multiple systems (`to: [a, b]`) but every lifecycle table speaks of a singular 'target': the semantics of ack/accept/decline/respond with N targets (all must respond? first responder wins? per-target folded state?) are undefined. ==> Either restrict exchange types to exactly one `to` entry (broadcasts excepted) — the simplest correct v1 rule — or define per-target sub-states and the aggregate fold; state the choice in §3.4 and enforce in the schema.
- [major/implementer-gaps] [03-domain.md §3.4.7 + 05-schemas.md §5.4 + 12-corner-cases.md CC-081/082] The chain that makes retire-blocking work is unspecified: how per-recipient acknowledge events on an announcement fold (announcements have no ack state in their state machine), and how the validator links a deprecation announcement to the specific contract version so it can check 'all registered consumers acked'. ==> Add to §5.2.1 a required machine-readable linkage on deprecation announcements (e.g. `deprecates: XC-…@version`), and define per-recipient ack tracking as part of the broadcast fold output.
- [major/implementer-gaps] [03-domain.md §3.5 (fold rule 3) + 12-corner-cases.md CC-032] Fold determinism ('same event set ⇒ same state, everywhere') is undermined by authorization depending on the mutable manifest: rule 3 drops unauthorized events, but whether authorization is evaluated against the manifest at event time or the current manifest is unspecified (CC-032 implies current, which makes historical folds change when membership changes). ==> Specify the fold's authorization input explicitly: e.g. 'authorization is evaluated against the manifest as of the event's commit (manifest history is part of the fold input)' or 'current manifest, and rebuild-equivalence is defined as equality under the same manifest snapshot'.
- [major/implementer-gaps] [05-schemas.md §5.2 (`origin`) vs 16-handoff-directive.md §16.2 vs 14-us-ac.md AC-1002.1] `origin` has three conflicting definitions: envelope says opaque local tracker IDs 'never paths'; §16.2 defines handoff `origin` as originating exchange/requirement IDs (B.8 uses an XW id); AC-1002.1 requires migrated artifacts to carry `origin` pointing at a legacy file path. ==> Split into distinct fields (e.g. `origin` = local tracker refs, `originating_exchanges[]` on handoff, `migrated_from` for the one-time migration) with per-field rules, and update AC-1002.1.
- [major/implementer-gaps] [02-actors.md §2.3 + 05-schemas.md §5.1 + A2-examples.md B.12] The `space.yaml` schema is never actually specified: §2.3's prose field list (machine actors + credential scopes, runtime channels, credential expiry per §9.3) and §4.2's comment (gates config, notification routes, vendored maintainer) do not appear in the B.12 excerpt, and no normative field table exists anywhere. ==> Add a normative `space/v1` field table to §5 (like §5.2), covering participants (incl. machine actors/credentials/expiry), gates overrides syntax, extra-fields allowlist, vendored maintainers, and notification-route fields if any live here.
- [major/implementer-gaps] [02-actors.md §2.1 + 05-schemas.md §5.6 + 07-client.md] How the binary determines the acting identity — `actor.kind`, `actor.name`, `actor.model`, `actor.session` — is nowhere specified (env vars? config? CLI flags? MCP client info?), yet `a2a new` must pre-fill it and §2.1 requires it on every artifact and event. ==> Specify the resolution order (e.g. explicit flags > A2A_ACTOR_* env vars > harness adapter defaults > config), the human-vs-agent determination rule, and the git author ↔ actor mapping recorded in space.yaml owners.
- [major/implementer-gaps] [05-schemas.md §5.7 + A2-examples.md B.1/B.7] The digest's 'canonical serialization (frontmatter normalized + body bytes)' is undefined — no key-ordering, YAML re-serialization, line-ending or Unicode normalization rules — and the string form is inconsistent (`sha256:` prefix in examples vs bare `<hex>` in §5.3; truncated display form unspecified). ==> Define canonicalization exactly (e.g. 'digest = SHA-256 of the artifact file's raw bytes as committed' — simplest and sufficient since files are immutable) and one string form (`sha256:<full-hex>`), used everywhere including `generated_from.source_digest`.
- [major/implementer-gaps] [05-schemas.md §5.3 + 07-client.md OP-213] `a2a contract verify-export` compares 'the regenerated content's digest' with the committed one, but what bytes are hashed is undefined for a multi-file export (contract.md? the schema/ directory tree? fixtures too? file ordering?). ==> Specify the export digest input precisely (e.g. sorted relative-path + file-content hash tree over `schema/` and `fixtures/`, excluding contract.md prose) and its encoding.
- [major/implementer-gaps] [05-schemas.md §5.4 + 03-domain.md §3.7 (G2) + 12-corner-cases.md CC-080] The G2 'CI compat check' has no specification: what artifact pair is compared, by what rules a JSON Schema diff is classified minor vs major, and what happens for `openapi-3.x`/`proto3`/`other` formats the binary doesn't deep-validate. ==> Scope it explicitly for v1: e.g. 'the check verifies declared-bump consistency for json-schema contracts using rule set X (field removed/type changed/constraint tightened ⇒ major); other formats: bump declaration + fixture back-validation only' — and mark deeper semantics as the CC-080 backstop.
- [major/implementer-gaps] [07-client.md §7.7 vs §7.1/§7.2 + 14-us-ac.md AC-301.2] The MCP tool list contradicts the parity invariant it sits next to: `a2a_search` and `a2a_contract_get` have no CLI counterpart in OP-2xx (no search command exists at all), while the claimed 'one-to-one with OP-203…OP-213' mapping omits sync/outbox and doesn't match the ten tools listed. ==> Publish one authoritative OP↔MCP-tool mapping table; add OP entries for search (with defined semantics) and contract-get, or remove them from §7.7.
- [major/implementer-gaps] [04-topology.md §4.2 (decisions/ CODEOWNERS)] 'CODEOWNERS = all required parties' for `decisions/` is not expressible: CODEOWNERS is static and path-based, while `required_approvers` vary per decision file. ==> Specify the v1 mechanism: e.g. CODEOWNERS lists all participants for `decisions/`, and the real quorum is enforced by V3 checking approve events against `required_approvers` — with the CODEOWNERS review being advisory only; state this in §4.2 and §10.3.
- [major/implementer-gaps] [07-client.md §7.3 (OP-217) + 15-rollout.md L0 + 13-testing.md T6] Distribution assumes fetching binaries/release assets from the product repo, but the product repo is private pre-open-source: how space CI (T6/V3) and `a2a update` authenticate to download the pinned validator/binary is unspecified. ==> Decide and document: e.g. binaries attached to the (private) product repo with a read-only fine-grained PAT stored as a space-repo secret, or a public releases-only repo; add the credential to §10.5 and the setup to §9.1.
- [major/implementer-gaps] [02-actors.md §2.1 + 07-client.md OP-205] Git commit conventions for binary-authored commits are unspecified: commit author/committer identity (agent PAT vs human git config), message format, and one-commit-vs-two for artifact+event — yet §2.1's attribution cross-check and CC-061's 'submit is atomic per artifact' depend on them. ==> Specify: commit author naming scheme per actor class, a conventional commit-message format carrying artifact/event IDs, and the atomicity unit (artifact + its lifecycle event in one commit).
- [critical/user-agent-gaps] [05-schemas.md §5.4 + 07-client.md §7.4 + 08-agent-protocol.md §8.2.7] The registered-consumer mechanism is unenforceable as specified: the primary registration path (declared dependency in the consumer's local `.a2a/config.yaml`) is invisible to every party that must act on it — the contract owner drafting the deprecation announcement, the space validator blocking retire (CC-081/082), and the hub computing the consumers-on-deprecated flag. ==> Make consumer registration space-visible: a small schema'd declaration file (e.g. `<system>/consumes/<XC-id>.yaml` with pinned version) committed to the consumer's own section, written by `a2a` when a dependency is declared; the local project config becomes a mirror/cache of it. Update 5.4's registered-consumer definition, 8.2 step 7, and the retire-block validator input accordingly.
- [major/user-agent-gaps] [08-agent-protocol.md §8.1 + 07-client.md §7.5 + 06-hub-service.md OP-102/OP-107] The daily-driver query semantics — `inbox --actionable`, `outbox --attention`, statusline 'new' and 'stale' — are never defined, and there is no specified per-consumer local read watermark to compute 'new' against. ==> Add a normative definition table: actionable = {submitted/no ack by me, responded awaiting my verify, disputed toward me, p1/blocking any state, gate pending}, attention = {my open items whose folded state changed since my stored read cursor, declined, disputed, stale}; specify a per-system read cursor stored in `.a2a/cache/` and advanced by explicit inbox reads; define the default staleness SLA (e.g. no event for N days) in §5 or space.yaml.
- [major/user-agent-gaps] [08-agent-protocol.md §8.5 vs 03-domain.md §3.4/§3.5 + 11-observability.md §11.3] The prescribed reminder mechanism ('use the existing exchange: an event `note`') is unimplementable: §3.5 events must encode a transition name and resulting-state claim, and no transition-free comment/nudge event exists in any lifecycle table; the notification matrix also has no row that would deliver a reminder to the counterparty. ==> Add a `remind` (or `note`) event type to §3.5 that carries no state change (fold: no-op, recorded on the thread), authorize it for the sender on any open exchange, add an OP-211 verb, and add a notification-matrix row (target's statusline: 'reminder on item you hold').
- [major/user-agent-gaps] [07-client.md §7.2 OP-211 vs 03-domain.md §3.4.2/§3.4.4 + CC-029] The CLI verb set does not cover the lifecycle tables: no `approve`/`reject` (decision, the G3 human gate), no `withdraw`/`satisfy` mapping for requirements, while `close` exists as a verb and in CC-029 but appears in no transition table (3.4.3 only has verify → closed). ==> Add a normative verb→event mapping table in §7.2 covering every transition of §3.4 (including approve, reject, withdraw, satisfy, publish, handoff verify-pass/fail), and reconcile 3.4.3 with CC-029 by adding an explicit `close` transition (responded → closed) distinct from per-response verification.
- [major/user-agent-gaps] [04-topology.md §4.2 + 05-schemas.md §5.7 + 03-domain.md §3.3] Resolution of pinned refs is unspecified: the layout stores exactly one `contract.md` per contract, yet refs pin `id@version` and `id#digest`, V5 must digest-check pinned refs, and the deprecation flow requires consumers to read the old version — nothing says how a version or digest maps to historical content. ==> Specify the version-resolution mechanism normatively in §4.2 or §5 (simplest: publishing a version = the submit tooling records commit SHA in the publish lifecycle event, and resolution = event lookup → git object), and add an OP for reading a ref at a pinned version/digest; consider `a2a contract diff <id> <v1> <v2>` for the deprecation-consumer loop.
- [major/user-agent-gaps] [07-client.md §7.7 vs §7.2] Discovery/search exists only as MCP tools (`a2a_search`, `a2a_contract_get`) with no CLI counterpart in the OP-2xx catalog — violating the plan's own CI-checked CLI/MCP parity invariant (7.1) and leaving CLI-only agents (Codex, CI) with no way to browse the space. ==> Add OP-2xx commands: `a2a search <query> [--type --space --state]` and `a2a contracts [--provider <system>]` (catalog of contracts across the space with version/stability/deprecation), mirroring the existing MCP tools; state that both operate on the local cache so they work hub-less.
- [major/user-agent-gaps] [07-client.md §7.2 OP-205 + 12-corner-cases.md CC-046] Push-race handling and event batching are unspecified for a shared repo where every system pushes to `main` continuously: `a2a submit` and each of the ~11 lifecycle verbs is 'validate → commit → push', but non-fast-forward push rejection (the common case, distinct from CC-046's merge-conflict case) has no specified retry behavior, and acknowledging 20 inbox items means 20 sequential commit+push round-trips. ==> Specify in OP-205/OP-211: mutating commands perform fetch → rebase → push with bounded retries, atomic per artifact/event (extending CC-061's atomicity claim); add a batch form (`a2a ack <id>... ` / `a2a triage` writing multiple event files in one commit) for session-start triage.
- [major/user-agent-gaps] [07-client.md §7.2 OP-201/OP-203] The two authoring entry points are specified interactively — `a2a init` is 'interactive/system setup' and `a2a new` 'opens for editing' — but the primary audience is non-interactive agents; no flag-driven/stdin/file-input mode is specified for the CLI path. ==> Specify non-interactive modes as normative: `a2a new <type> --field k=v ... --body-file -` (or: write draft to a path and print it, never open $EDITOR unless a TTY and flag given), and `a2a init --system <id> --space <url>` flags covering everything the interactive path asks.
- [major/user-agent-gaps] [03-domain.md §3.4 + 07-client.md OP-203] Where draft artifacts live is unspecified: every lifecycle starts at `draft`, but the plan never says whether drafts are committed/pushed to the space (and thus visible in the counterparty's inbox and hub index) or exist only locally until submit. ==> Decide and state it: recommended — drafts are local-only (under `.a2a/` staging, surviving sessions), the space only ever contains submitted/published artifacts, the `draft→submit` transition collapses into the first commit, and inbox/notification queries are defined over committed artifacts only; adjust 3.4 tables and the validator's draft-warning accordingly.
- [critical/security] [10-security.md §10.5 (credentials table, project-agent row) + §2.2 + §4.2] The entire single-writer authz model assumes a GitHub fine-grained PAT can be scoped to write only one section (subdirectory) of a repo. It cannot: fine-grained PATs grant repo-level Contents:Write with no path scoping, and GitHub rulesets/branch-protection cannot express 'actor A may modify only path axon/**'. So every project agent's 'section-scoped' PAT can in reality write to the ENTIRE space repo, including other systems' sections. ==> Drop the claim that PATs enforce section isolation. Either (a) accept that real per-section write isolation is impossible on a shared GitHub repo and rely ONLY on the PR+CODEOWNERS path (killing the direct-push fast path — see the paired finding), or (b) move section isolation to the hub-mediated write path (D-002) now rather than deferring it, or (c) one-repo-per-section topology. Rewrite §10.5, §2.2, §10.3, and T-2/T-5 to state that in v1 write-scope is a trust convention detectable after the fact, not a hard boundary.
- [critical/security] [04-topology.md §4.2 (line: 'Sections whose gates don't apply push directly to main') + §5.5 (V3 row 'merge/push blocked')] The plan wants BOTH direct push to main for non-gated autonomous events AND CODEOWNERS/V3 enforcement of section ownership and gates on the same branch. On GitHub these are mutually exclusive: branch protection/CODEOWNERS review and required status checks only bind the PR path; anyone permitted to push directly to main bypasses CODEOWNERS and all status checks. You cannot gate by path — only by branch. ==> Resolve the dilemma explicitly. If autonomy (direct push, R-001/§3.7 'everything else agents do without humans') is kept, state that CODEOWNERS and V3 are advisory/detective on the direct-push path, not preventive, and that section forgery is a residual v1 risk. If enforcement is kept (PR-only main), state that every ack/accept/respond/verify becomes a PR requiring code-owner review, and re-cost the autonomy story. §5.5's V3 'push blocked (branch protection)' is false for direct pushes and must be corrected.
- [critical/security] [11-observability.md §11.1 (dashboard renders titles/bodies/notes) + 07-client.md §7.6 (local HTML) + 05-schemas.md §5.2 (title free text ≤120)] Artifact-controlled strings (title, body markdown, event note, decline reason, interim_behavior) authored by a partner org are rendered into the hub dashboard and local HTML with no stated output sanitization/escaping. The hub dashboard is served to an authenticated operator whose session effectively commands the all-space read PAT and the hub tokens. ==> Add an explicit output-encoding requirement: all untrusted artifact fields MUST be HTML-escaped; markdown bodies MUST be rendered through a sanitizing renderer (no raw HTML/script/embedded resources), consistent with the CSP-style 'no external requests' claim. Make this a §10 enforcement point and a T1/T3 test (malicious-title/body fixture must render inert). Same rule for statusline text injected into an agent's context.
- [critical/security] [10-security.md §10.5 (hub row: read-only PAT, all spaces) + §10.2 T-6] The hub holds one read PAT covering ALL spaces it serves, aggregating cross-org confidential exchange content behind a single VPS. T-6 rates hub compromise as 'worst case = wrong notifications/dashboard, detectable,' which is wrong: hub compromise leaks the confidential content of every org in every space, plus the static bearer tokens in hub.yaml. ==> Re-rate T-6 to reflect that hub confidentiality blast radius = all spaces, and make it a first-class asset. Minimize: per-space read tokens rather than one fleet PAT; consider that spaces with cross-org private content may need to be excluded from hub mirroring; document that 'restricted' content is exposed to the operator via this PAT (ties to Q-001). Add hub-host hardening to the v1 threat mitigations, not just public mode.
- [major/security] [02-actors.md §2.1 (attribution 'cross-checked against the git commit author') + 12-corner-cases.md CC-064] The claimed mitigation that forged attribution is 'detectable in audit (commit author vs claimed actor)' has no ground truth in v1. Git commit author/email is arbitrary attacker-controlled metadata (git config), and commits made via a project's PAT appear as the single PAT-owning identity regardless of whether a human or an agent acted. Signed commits are deferred to public mode (10.6). ==> State plainly that in v1 actor attribution (kind/name/model/session AND the git author line) is self-asserted and unverifiable, so it is provenance-for-humans, not a security control. Do not lean on 'commit author cross-check' for G3/G5 integrity (see next finding). If any gate must be trustworthy in v1, require signed commits/events for those specific events now rather than deferring all signing.
- [major/security] [03-domain.md §3.7 (G3, G5) + 10-security.md §10.3 (approve decision {V3})] G3 (multi-party decision approval) and G5 (classification override) are enforced only by the validator checking a self-declared field actor.kind: human inside an event file the agent itself writes. Unlike G1/G2/G4 (which route through real GitHub PR review by a human account), G3/G5 have no mechanical human-in-the-loop — an agent forges kind:human and the decision folds to approved. ==> Route decision approval and classification-override events through the same PR-review mechanism as G1/G2/G4 (a real human GitHub action is the only v1 primitive that actually proves a human acted), or require signed events for G3/G5 in v1. Remove the implication in §3.7/§10.3 that a validator field-check enforces a human gate.
- [major/security] [10-security.md §10.4 (forbidden payloads / secret-scan) + §5.3 (contract payload files) + §5.5 (V2 local)] The forbidden-payload/secret scan is presented (S-2, T-4, AC-203.1) as an enforcement boundary, but it is heuristic pattern-matching and its scope is undefined for the highest-risk locations: contract schema/ files, fixtures/valid|invalid/ (arbitrary 'golden data', a natural exfil channel), and docs/. For non-json-schema contracts §5.3 says the binary only checks that fixtures exist and digests match — i.e. it never inspects that payload content at all. V2 is local and trivially bypassed by pushing directly. ==> Specify exactly which paths the secret/PII/forbidden-payload scan covers (must include provides/**/schema, fixtures/**, docs/**, event notes, not only .md envelopes), and state that it is best-effort detection, not prevention — a compromised sender skips V2 and V3 flags after the fact. Downgrade S-2/T-4/AC-203.1 claims accordingly. Acknowledge that regex cannot detect encoded secrets, UUID-shaped keys, private algorithms, or end-user PII.
- [major/security] [07-client.md §7.3 (a2a update) + 10-security.md T-8] a2a update auto-pulls the latest release verifying only 'checksums,' with binary signing/provenance punted to 'a product-repo CI concern' (T-8). Checksums served from the same release channel as the binary provide no authenticity — a compromised channel serves matching binary+checksum. The binary IS the security kernel (validator, gate checks, secret scan, credential handling) for the whole fleet. ==> Require signed releases (cosign/minisign or Sigstore) with the public key pinned in the binary, verified before self-update, as a v1 item — not deferred. State that a2a update must fail closed on signature mismatch. Keep min_binary_version but recognize it does not provide authenticity.
- [major/security] [10-security.md §10.5 (hub API clients: static bearer tokens in hub.yaml) + 06-hub-service.md §6.3 (force-push → full re-index; webhook → git fetch)] Hub API/dashboard access uses static, non-expiring bearer tokens stored in hub.yaml with no rotation policy (§9.3 rotation covers GitHub PATs only). Separately, the ingest pipeline does unthrottled git fetch on every webhook and does a full re-index on every force-push (F-7) with no rate limiting — a participant who can push can force-push in a loop to pin the hub in re-index (DoS). ==> Give hub bearer tokens an expiry + rotation runbook and store them hashed, not plaintext, in hub.yaml (or reference a secret store). Add rate limiting / debouncing to webhook-triggered fetch and to force-push re-index, and cap per-push content size at ingest. Add these to §9.3 and §6.3.
- [critical/consistency] [03-domain.md §3.4.3/§3.4.6 vs 12-corner-cases.md CC-029 vs 07-client.md OP-211] Closure semantics are specified three contradictory ways: §3.4.3 says `verify` transitions responded→closed, CC-029 says the parent 'stays responded until sender's close event' (a `close` event exists in no transition table), and OP-211 lists both `verify` and `close` as separate verbs; E2E-1 additionally uses 'verify → close' for a requirement whose only closing event is `satisfy`. ==> Pick one model and propagate: either (a) per-response `verify` events plus a distinct sender `close` transition responded→closed added to the §3.4.3 table, or (b) verify closes and partial responses are handled by verify carrying a response ref with closure only when the sender verifies with a `final` marker. Update CC-029, OP-211, E2E-1, and rename/define the requirement path (`satisfy`) consistently.
- [critical/consistency] [03-domain.md §3.4.7 vs §3.5 fold rule 2; breaks 05-schemas.md §5.4, CC-081/082, E2E-4] Targeted announcements with `ack_requested: true` are said to produce acknowledge events, but the announcement state machine has no acknowledge transition and no acknowledged state, so per fold rule 2 every such ack event is an illegal transition that must be ignored and flagged. ==> Define ack semantics for broadcasts explicitly: either a per-recipient ack event class that does not claim a state transition (exempted from fold rule 2, folded into a per-recipient ack set), or add an acknowledge row to §3.4.7 with per-recipient state. Reference it from §5.4 retire preconditions.
- [major/consistency] [03-domain.md §3.4.1 (retire) vs 05-schemas.md §5.4 / CC-082] Retire conditions contradict: §3.4.1 allows retiring with remaining registered consumers via a human gate ('only after sunset and zero registered consumers, else gate'), while §5.4 and CC-082 say retire is strictly validator-blocked until all registered consumers acked, with CC-082 stressing 'the block is the feature' and no override. ==> Resolve to one rule (suggest: strict block on un-acked consumers, gate only for the zero-consumer-but-pre-sunset case) and align §3.4.1's actor/condition column, §5.4, CC-082 and AC-202.2 verbatim.
- [major/consistency] [03-domain.md §3.4.4 vs 13-testing.md E2E-5 and §3.8] `rejected` is a dead-end state for decisions — no transition leaves it and `supersede` is only legal from `approved` ('new approved decision only') — yet E2E-5 requires 'one reject → revision → approve'. ==> Add transitions: `rejected → supersede → superseded` (successor decision) and/or `rejected → revise → draft`, plus a draft/proposed withdraw for the author. Update E2E-5 to name the exact path used.
- [major/consistency] [03-domain.md §3.4.5 vs 16-handoff-directive.md §16.3 and AC-1101.2] §16.3 mandates that a resubmitted handoff is 'a new revision on the same thread, superseding the rejected one', and AC-1101.2 tests the predecessor link, but the handoff transition table (§3.4.5) has no supersede row (and `rejected` has no outgoing transitions). ==> Add `rejected → supersede → superseded` (and consider producer `withdraw` and receiver `decline`) to §3.4.5; note that the 'any open supersede' rule of §3.4.3 does not apply to handoffs as written.
- [major/consistency] [12-corner-cases.md CC-023 vs 03-domain.md §3.4.3/§3.5] CC-023 says the sender 'MAY still verify as courtesy-closure' of a cancelled/superseded exchange, but `verify` is only legal from `responded`; from `cancelled`/`superseded` both the response and the verify are illegal transitions the fold must ignore and flag. ==> Either delete the courtesy-verify sentence (fold flags are the record) or add an explicit legal transition (e.g. `superseded → verify → superseded` recorded as annotation) and say which folded state results.
- [major/consistency] [05-schemas.md §5.2 `origin` vs 16-handoff-directive.md §16.2 and AC-1002.1] The field name `origin` has two conflicting definitions: envelope base §5.2 says 'opaque local tracker IDs … never paths', while §16.2 defines handoff `origin` as 'originating exchange/requirement IDs' (B.8 uses an exchange ID), and AC-1002.1 requires migrated artifacts to carry 'origin pointing at its legacy path' — a path, which §5.2 forbids. ==> Split the concepts: keep `origin` for opaque local tracker IDs, add a distinct handoff field (e.g. `originating_exchanges[]` of artifact IDs), and change AC-1002.1 to reference legacy files by an opaque marker (e.g. repo-relative name recorded in the migration note), not a path in `origin`.
- [major/consistency] [05-schemas.md §5.2.1 handoff row vs 16-handoff-directive.md §16.2 / A2-examples.md B.8] §5.2.1 defines the handoff evidence block as (`verification`, `artifacts`, `limitations`), but §16.2 — the normative extension — and the B.8 golden fixture use `deliverables[]` (plus `acceptance_criteria`, `env_requirements?`, `origin`), with no field named `artifacts`. ==> Rewrite the §5.2.1 handoff row to enumerate exactly §16.2's fields: `deliverables[]`, `verification`, `acceptance_criteria`, `limitations[]`, `env_requirements?`, plus the resolved origin field.
- [major/consistency] [A2-examples.md B.8 vs 16-handoff-directive.md §16.1 and §5.2 ref grammar] The B.8 handoff fixture violates the handoff's own schema-checkable precondition — §16.1(2) requires `refs` non-empty and pinned, but B.8 has no `refs` key at all — and its deliverable ref `XH-axon-20260810-f3s5#body` uses a `#body` fragment form absent from the §5.2/§3.8 ref grammar (and is a self-reference). ==> Add pinned `refs` to B.8, replace `#body` with a defined ref form (or extend the ref grammar to define it), and decide whether §16.1(2) means envelope `refs` or `deliverables[].ref`.
- [major/consistency] [07-client.md OP-211 vs 03-domain.md §3.4 event vocabulary] The OP-211 lifecycle-verb list (`ack/accept/decline/start/block/respond/verify/dispute/close/cancel/supersede`) does not cover the event vocabulary of §3.4: `withdraw` (3.4.2), `satisfy` (3.4.2), `unblock` (3.4.3), `verify-pass`/`verify-fail` (3.4.5), `publish` for standing/broadcast types — while it includes `close`, which no transition table defines. ==> Regenerate OP-211 mechanically from the union of §3.4 transition names, one verb per event, and remove or define `close` per the resolution of the closure-semantics finding.
- [major/consistency] [15-rollout.md L0/L1 exit criteria vs 14-us-ac.md AC-102.1, AC-201.2, AC-302.1] Phase exits cite hub-dependent ACs before the hub exists: L0 requires AC-102.1 ('the hub serves it with no hub-side config change'), L1 requires US-201 and US-302 ACs including AC-201.2 (V4 hub validation identical) and AC-302.1 (folded state matches 'on binary and hub', T4) — but the hub ships in L3. ==> Split the affected ACs into phase-scoped parts (e.g. AC-102.1a git/CODEOWNERS at L0, AC-102.1b hub-pickup at L3) or annotate L0/L1 exits as 'excluding hub clauses, re-verified at L3'.
- [major/consistency] [03-domain.md §3.2 vs 12-corner-cases.md CC-009] §3.2 normatively states 'the validator rejects a question living inside an announcement', while CC-009 for the identical scenario says 'schema can't fully see prose' and the enforcement is template guidance plus recipient decline. ==> Rewrite §3.2 to claim only what is checkable (structural markers, e.g. per-type forbidden envelope fields), and make CC-009 the normative behavior for prose-level smuggling.
- [major/consistency] [08-agent-protocol.md §8.5 (stale-item reminder) vs 03-domain.md §3.5] §8.5 instructs agents to send a reminder as 'an event `note`' on the existing exchange, but §3.5 requires every event to carry a transition name and resulting-state claim; no note-only/no-op event exists, so a reminder event is an illegal transition the fold must ignore and flag. ==> Define a non-transition annotation event kind (e.g. `transition: note`, state claim = current state, exempt from rule 2) in §3.5 and its schema in §5, or change §8.5 to a different reminder mechanism.
- [major/consistency] [04-topology.md §4.2 decisions/ vs 05-schemas.md §5.2 `from` rule and CC-002] Envelope rule '`from` MUST match owning section (validator + authz)' plus CC-002 ('from ≠ owning section → V2/V3 reject') is violated by every decision artifact, which lives in the shared `decisions/` directory while carrying `from: <drafting system>` (B.6). ==> Add an explicit exception to §5.2 and CC-002: for `type: decision`, path authority is `decisions/` and `from` is the drafting system, with write authorization via the decision flow (CODEOWNERS = required parties).
- [major/consistency] [04-topology.md §4.2 (direct push to main) vs 02-actors.md §2.2 / 10-security.md §10.3 / CC-060] §4.2 allows ungated sections to 'push directly to main', but the claimed cross-section write enforcement (CODEOWNERS + branch protection, CC-060, AC-101.2) only operates on the PR path — GitHub CODEOWNERS review and required checks cannot path-scope direct pushes, so a member can push into a foreign section whenever direct push is enabled. ==> Decide the mechanism explicitly: either all writes via PR (with auto-merge for ungated changes to keep friction low) or accept push-then-flag for ungated writes and downgrade the §10.3/{CODEOWNERS} claims and CC-060 accordingly.
- [critical/ops-realism] [04-topology.md §4.2 (rules) + 17-decisions.md D-002 + 10-security.md §10.3] The core write-enforcement story is broken: with direct push to main allowed for ungated changes, CODEOWNERS enforces nothing — GitHub evaluates CODEOWNERS only as a required-review rule on pull requests, and branch protection has no per-path push restriction. ==> Pick and specify one of the two mechanisms GitHub actually has: (a) require PRs for ALL changes via a branch ruleset with required-approvals=0 plus require-code-owner-review, putting CODEOWNERS entries only on gated paths (provides/, decisions/, space.yaml) so ungated PRs auto-merge on green CI with zero humans; or (b) keep direct push and add per-section push/branch rulesets using 'restrict file paths' (pattern <system-id>/**) with a bypass list containing only that system's team. Update D-002, §4.2, §10.3 and CC-060 to name the chosen mechanism explicitly.
- [critical/ops-realism] [04-topology.md §4.5 + 09-human-ops.md §9.1 + 15-rollout.md L0] Branch protection, required code-owner review, and rulesets are unavailable on private repositories under GitHub Free — spaces are private by design (privacy boundary = repo, D-003), and the plan never states the paid-plan prerequisite. ==> Add an explicit hosting prerequisite to §4.5 and the §9.1 space-admin runbook: operator org MUST be on GitHub Team (or repos public), verified during space creation; add a check to `a2a doctor`/onboarding that protection rules are actually active, not just configured.
- [major/ops-realism] [10-security.md §10.5 + §10.2 T-2] Fine-grained PATs scope to repositories and permission types only, never to paths — the stated write scope 'write = own sections by CODEOWNERS' does not exist, so a leaked agent write-PAT can write the entire space, not one section. ==> Rewrite T-2 and §10.5 to state the real containment boundary (whole space repo per PAT) and derive section containment only from the mechanism chosen for the direct-push fix (push rulesets with team bypass, or PR-only writes). Note also that fine-grained PATs against org-owned repos require the org to allow member fine-grained PATs and cap at 1-year expiry — add to §9.3.
- [major/ops-realism] [05-schemas.md §5.5 (V3 row) + 12-corner-cases.md CC-001 + 01-vision.md S-2] V3's 'on failure: merge/push blocked (branch protection)' is impossible for direct pushes — GitHub has no pre-receive hook for hosted repos; required status checks gate only PR merges, and CI on a push runs after the commit is already on main. ==> Either adopt PR-only writes (making V3 a genuine merge gate for everything), or downgrade V3's contract for direct pushes to detect-and-flag and add a specified remediation flow (auto-revert PR or hub alert with runbook). Restate S-2 to match the enforceable reality.
- [major/ops-realism] [10-security.md §10.3 + 05-schemas.md §5.5 (V2/V3 authz class)] The authz validation checks only content self-consistency (`from` matches the section path) — nothing binds the actual pushing identity (github.actor / PR author / token owner mapped via space.yaml) to the section being written. ==> Specify in §5.5 that V3 (CI) MUST verify the push/PR actor (github.actor, which is token-derived and not spoofable) against the space.yaml participant block for every changed path, and that V4 flags commits whose GitHub author has no mapping to the section's system. Add the github-handle→system mapping as a required space.yaml field.
- [major/ops-realism] [05-schemas.md §5.4 + 12-corner-cases.md CC-081 + 14-us-ac.md AC-202.2] 'Registered consumer' is defined partly by declarations in each consumer's private project config (§7.4), which the space CI validator and hub cannot read — so retire-blocking (CC-081) and deprecation targeting are uncomputable as specified. ==> Move the consumer registry into the space: a small declared-dependencies artifact (or field in the consumer's section) that `a2a` writes when a project registers a contract dependency (8.2 step 7 already commits — make its target the space, not only local config). Redefine 'registered consumer' in §5.4 over space-visible data only.
- [major/ops-realism] [03-domain.md §3.7 (G1/G2) + 05-schemas.md §5.4 + 14-us-ac.md AC-202.1] Conditional human review — required only for first publish and breaking bumps, not for minor/patch — cannot be expressed in branch protection or CODEOWNERS; review requirements are path-based and unconditional. ==> Specify the mechanism: a required status check in the space CI that computes the version diff and fails unless the PR carries an approving review from the owner (queried via the API) when the bump is major or first publish. Document that ungated contract changes then merge review-free with green CI. Anchor this in §5.5 as part of V3's policy class.
- [major/ops-realism] [07-client.md §7.5 + 01-vision.md S-3 + 14-us-ac.md AC-502.1] The statusline freshness model is self-contradictory and has no owner for cache refresh: 'always cache-first, never blocks the prompt, <100 ms' cannot coexist with 'hub OP-108 fast path ~seconds fresh' or with a 5-minute TTL git fetch unless some component performs asynchronous refresh — which is never specified. ==> Specify the refresh mechanism explicitly in §7.5: statusline render reads cache only; when cache age > TTL it spawns a detached background refresh (hub OP-108 or git fetch) whose result lands in the cache for the next render. State the resulting worst-case staleness (TTL + one render interval) and align S-3/AC-502.1 numbers with it.
- [major/ops-realism] [07-client.md §7.3 (D-013) + 10-security.md T-8] `a2a update` verifies only checksums fetched from the same release channel, which authenticates nothing — a compromised product-repo release (or maintainer account) yields silent remote code execution on every participant machine and the hub VPS. ==> Require signed releases: embed a signing public key (minisign/cosign) in the binary and have `a2a update` verify the signature before swapping; document key rotation in §9. At minimum, pin expected release checksums in space.yaml alongside min_binary_version so an update requires a reviewed manifest change.
