{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "envelope/v1/response.schema.json",
  "title": "Envelope: response (v1)",
  "description": "No `category` field per the-plan §5.2.1 (dash entry, same as decision/handoff) — this schema and base both omit `category`, so unevaluatedProperties:false rejects it structurally if present.",
  "allOf": [{ "$ref": "base.schema.json" }],
  "type": "object",
  "properties": {
    "type": { "const": "response" },
    "id": { "pattern": "^XS-" },
    "parent": { "type": "string" },
    "result": { "type": "string", "enum": ["answered", "delivered", "partial", "cannot"] },
    "to": {
      "description": "Exchange type (X class, not decision): exactly one target system.",
      "type": "array",
      "items": { "type": "string" },
      "minItems": 1,
      "maxItems": 1
    }
  },
  "required": ["parent", "result"],
  "patternProperties": { "^x_": true },
  "unevaluatedProperties": false
}
