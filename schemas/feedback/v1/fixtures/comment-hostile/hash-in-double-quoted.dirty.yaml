feedback: v1
id: fb-20260819-11aa22 # auto-filled: fb-YYYYMMDD-<6 hex>
kind: bug # bug | feature | docs | friction | protocol
severity: minor # blocker | major | minor
title: "a bug in the # channel" # 8-80 chars, specific
summary: "The `#` inside the double-quoted title above is the reporter's own text, not a comment, and a reader that strips it eats data."
context:
    a2a_version: 0.23.0 # auto-filled
    os_arch: darwin/arm64 # auto-filled
    surface: cli
evidence:
    steps:
        - "Post in the # channel."
    expected: "the title survives intact"
    actual: "the title was truncated at the #"
checks:
    docs_consulted: true
    grounded_in_real_work: true
    not_space_specific: true
    no_sensitive_content: true
    duplicates_checked: true
status: new
