feedback: v1
id: fb-20260708-8192a3
kind: friction
severity: minor
title: "a2a validate errors give no line number for the failing field"
summary: >-
  A schema violation report named the field but not where it lives in the
  file; on a long envelope I had to binary-search the frontmatter by eye to
  find the offending key, costing several extra round-trips this session.
context:
  a2a_version: v0.1.1
  os_arch: linux/amd64
  surface: cli
  command: a2a validate draft.md
proposal: "carry a line/column position through from the YAML decode into the violation"
checks:
  docs_consulted: true
  grounded_in_real_work: true
  not_space_specific: true
  no_sensitive_content: true
  duplicates_checked: true
status: new
