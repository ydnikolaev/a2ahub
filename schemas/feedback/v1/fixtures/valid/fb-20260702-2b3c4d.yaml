feedback: v1
id: fb-20260702-2b3c4d
kind: bug
severity: blocker
title: "a2a validate --ci exits 0 on a malformed frontmatter block"
summary: >-
  A draft with an unterminated frontmatter fence (missing closing `---`) was
  fed to `a2a validate --ci` and it exited 0 with no machine line, instead of
  the documented POL-002 rejection.
context:
  a2a_version: v0.1.1
  os_arch: linux/amd64
  surface: cli
  command: a2a validate --ci draft.md
  error_code: POL-002
evidence:
  steps:
    - "author a draft with `---\\nschema: envelope/v1` and no closing fence"
    - "run `a2a validate --ci draft.md` against it"
    - "check the exit code and stdout"
  expected: "exit 1, one machine line citing POL-002"
  actual: "exit 0, no output"
checks:
  docs_consulted: true
  grounded_in_real_work: true
  not_space_specific: true
  no_sensitive_content: true
  duplicates_checked: true
status: new
