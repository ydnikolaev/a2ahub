feedback: v1
id: fb-20260704-4d5e6f
kind: feature
severity: minor
title: "no dry-run flag for a2a sync"
summary: >-
  Before letting `a2a sync` rewrite my local mirror I wanted to see what it
  would fetch/prune without committing to it; there is no `--dry-run` and I
  had to snapshot the directory first to compare afterward.
context:
  a2a_version: v0.1.1
  os_arch: linux/amd64
  surface: cli
  command: a2a sync
proposal: "a2a sync --dry-run: print the fetch/prune plan, change nothing on disk"
checks:
  docs_consulted: true
  grounded_in_real_work: true
  not_space_specific: true
  no_sensitive_content: true
  duplicates_checked: true
status: new
