feedback: v1
id: fb-20260716-091a2b
kind: feature
severity: minor
title: "status is accepted, not new — invalid at submit time"
summary: >-
  This report is otherwise well-formed but carries a hub-mutated status;
  `status` MUST be `new` at submit (§T2) — the JSON schema itself permits
  every enum value since status transitions are the hub's job, not the
  schema's.
context:
  a2a_version: v0.1.1
  os_arch: darwin/arm64
  surface: cli
checks:
  docs_consulted: true
  grounded_in_real_work: true
  not_space_specific: true
  no_sensitive_content: true
  duplicates_checked: true
status: accepted
resolution: "backlog fb-20260716-091a2b; ships with a future phase"
