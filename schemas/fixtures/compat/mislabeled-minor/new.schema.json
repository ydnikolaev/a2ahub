{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "compat-fixture/mislabeled-minor/widget-1.1.0.schema.json",
  "title": "Widget payload (contract v1.1.0) — mislabeled minor: description is now REQUIRED (breaking, CC-080)",
  "type": "object",
  "properties": {
    "name": { "type": "string" },
    "description": { "type": "string" }
  },
  "required": ["name", "description"],
  "additionalProperties": false
}
