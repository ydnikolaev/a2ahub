# lane-ungated.txt — P12 (docs/features/archive/agent-ops-2026-07) D-3/D-6.
#
# The explicit list of repo paths NO declared gate claims, on purpose. A
# path lands here only after checking every real declaration and finding
# none reads it — never as a way to silence a refusal that just hasn't been
# investigated yet.
#
# `go run internal/lane/lanecheck.go --verify` (wrapped by
# scripts/check-lane-declarations.sh) reads this file relative to LANE_ROOT
# (default: repo root). Its absence is NOT an error — an empty ungated list
# is the correct starting point, not a missing dependency (see
# lanecheck.go's readUngatedList).
#
# Format: one repo-relative glob per line (the same grammar
# internal/lane/glob.go's Match uses — "**" crosses path separators,
# everything else is single-segment). "#" starts a comment; blank lines are
# ignored. Every entry REQUIRES a reason: put it as a "#" comment on the
# line directly ABOVE the glob it explains, e.g.:
#
#   # Swift package; no gate in this repo's make-ABI reads it.
#   integrations/macos-notifier/**
#
# This file intentionally ships EMPTY at the end of P12/W2. The lead
# adjudicates the coverage residue inline (plan D-6) before populating it —
# an agent filling this in on day one is how the gate goes toothless on
# arrival, which is explicitly not this session's call to make.

# ── Adjudicated 2026-08-06, P12/W2 lead pass. 318 refusals, by root. ──
#
# The test applied to every entry below: is there a gate inside `make check`
# that reads this path? Not "could there be one" — is there. Where the answer
# was "no, and there should be", the row went to docs/validator-backlog.md
# instead of here (see feedback/** below).

# Authored prose. feature-lint reads only the README/tracker shape of an epic
# directory and declares exactly that; specs, plans and audits are judgement
# nothing mechanical can check.
docs/features/README.md
docs/features/**/specs/**
docs/features/**/plans/**
docs/features/**/audits/**
docs/features/**/plan.md
docs/features/**/design-handoff.md
# seam.md — an epic's frozen inter-phase contract, quoted verbatim by its
# plans (agent-exchange-2026-08 is the first). Its Go signatures are prose
# until the phase implements them, so nothing compiles or checks them here;
# what enforces it is the plan cross-review, which is judgement.
docs/features/**/seam.md
# threat-model.md — what an epic's own additions make abusable, per threat,
# each landing in a named phase. Every control it names is enforced by the
# phase that owns it (a refusal, a ceiling, a rendering), so what is left
# here is the reasoning about WHY those controls exist — judgement, like the
# specs beside it.
docs/features/**/threat-model.md
# epic-backlog.md — real problems found DURING an epic's implementation that
# are neither HIGH enough to stop a wave nor small enough to fix in it. Each
# row carries what is wrong, what closing it costs, and the evidence, so the
# decision to defer stays reviewable. It is a ledger of judgement, like
# docs/backlog.md and docs/validator-backlog.md above, and nothing mechanical
# can check whether a deferral was the right call.
docs/features/**/epic-backlog.md

# Two integrations with their own toolchains — a Swift package and a VS Code
# extension with its own npm test suite. Nothing in this repo's make-ABI can
# build or judge either; `make check` never enters this tree.
integrations/**

# The harness the agents LOAD — skills, rules, doctrines, lock files, provider
# surfaces. Delivered by `mate fleet pull`, never committed here (see
# .gitignore's DENY block), and no gate inside `check` reads it.
# .agents/scripts/** is the exception and is claimed by harness-teeth.
.claude/**
.mate/**
.codex/**
.sporo/**
.agents/skills/**
.agents/rules/**
.agents/doctrine/**

# Archived audit records and inbox prose under the architecture plan. The
# LIVE plan corpus — docs/the-plan/plan/** — is claimed by
# operational-confidence-guard and skill-citations; these two are history.
docs/the-plan/audit-*/**
docs/the-plan/inbox/**

# Dated conformance measurements sitting beside the plan rather than inside
# it — `docs/the-plan/conformance-<date>.md`. Same class as the audit records
# above and ungated for the same reason: the file is a RECORD of what was true
# on a day, so the only invariant a gate could hold it to is "it still parses",
# which proves nothing about whether the measurement was honest. What IS gated
# is everything it cites — the tests it names are resolved by the ordinary Go
# gates, and the corner-case half of the same question lives in
# cc-coverage.yaml, where TestCCCoverageAccountsForEveryDeclaredCornerCase does
# hold it to the plan. If a later measurement becomes an INPUT — a release
# checklist reading its verdict column, a gate diffing today's answer against
# it — that is the day it earns a claim, and this entry gets narrowed.
docs/the-plan/conformance-*.md

# Internal HTML presentations — one-off decks written to be looked at once,
# not published and not read by anything. No gate can hold them to an
# invariant beyond "the file parses", which proves nothing about whether the
# deck says anything true. The day one of these becomes PUBLISHED material it
# stops being this class: web/public/** is claimed, and a deck that ships to
# readers belongs there, under the gates that judge published pages.
docs/presentations/**

# Vision prose — landscape reads and where-do-we-go-next arguments about
# systems OUTSIDE this repository (other agent runtimes, plugin
# marketplaces, registries). No gate can judge them: the load-bearing claims
# are about third-party manifests and submission processes, which no local
# check can reach and which go stale on their owners' schedule, not ours.
# Each row therefore carries its own source URL and verification date inline,
# the SourceURL/VerifiedOn idiom internal/surface/registry.go already uses.
# The day a vision doc's claim becomes a COMMITMENT it stops being this
# class: it becomes a spec under docs/features/**, which is gated.
docs/vision/**

# Operator prose. publish-to-public.sh is the exception, claimed by
# harness-teeth, because it carries its own --teeth self-test.
docs/runbooks/**

# Inbound notes, generated reports, doc templates and the two backlogs. Read
# by people and by skills, never by a gate.
docs/inbox/**
docs/reports/**
docs/_templates/**
docs/backlog.md
docs/validator-backlog.md
docs/handoff-*.md

# Repo furniture and release/scan configuration. `.goreleaser.yaml`,
# `.gitleaks.toml` and `.govulncheck-allow.txt` are consumed by tools that run
# OUTSIDE `check` (goreleaser at tag time, gitleaks in CI, `make vulncheck`
# which needs the network and is deliberately not in the ceiling).
SECURITY.md
NOTICE
LICENSE
.gitignore
.gitleaks.toml
.goreleaser.yaml
.govulncheck-allow.txt
.github/dependabot.yml

# A single seed document for other repositories to install this project's
# skill; nothing here reads it.
seeds/a2a.md

# The repo's own feedback corpus is now CLAIMED — `feedback/inbox/**` and
# `feedback/backlog.yaml` are declared by scripts/check-feedback-corpus.sh
# (agent-exchange-2026-08 P9), which validates every record split by its
# status, refuses an id that disagrees with its filename, refuses a release
# note citing a report the corpus still calls unread, requires every non-`new`
# resolution to name a referent that RESOLVES, and requires the WIP ledger to
# agree with the inbox. What used to stand here was the whole of `feedback/**`
# with the note "NO gate inside `check` reads this copy" — true when written,
# and the exact hole the P12 coverage refusal existed to surface.
#
# What is left is the residue, narrowed rather than deleted, because AC8 asks
# for the remainder to be named with its own reason:
#
# feedback/digest.md — the append-only triage history, written by
# `feedback.ApplyVerdicts` and never read back by anything. It is a log, not a
# state: nothing derives a verdict from it, so there is no invariant a gate
# could hold it to beyond "it grew", which proves nothing. If it ever becomes
# an input — a report generator, a released changelog — that is the day it
# earns a claim, and this entry gets deleted rather than narrowed again.
feedback/digest.md

# macOS Finder droppings. Not repo content; appears only in the private
# harness roots the universe walks because they are untracked by design.
**/.DS_Store

# Resolved backlog rows, moved out of docs/backlog.md on 2026-08-12 because
# that file's own header asks for it (102 of its 138 rows were already `[x]`).
# Nothing reads this file: `feedback-corpus` claims docs/backlog.md because it
# resolves the `refs:` a feedback record points at, and a CLOSED row is not a
# referent — every accepted record's row lives in the live file. The archive is
# a reading surface for humans and a place where a measurement can be found
# again, not an input to any verdict. If a gate ever learns to check that a
# closed row's claim is still true, this entry gets deleted rather than kept.
docs/backlog-archive.md
